/**
 * Gradiente do hero da Home (loading + carregado) — constante única (T1).
 * Antes inline duplicado em LoadingScreen e na hero-area.
 */
export const HERO_BACKGROUND = "linear-gradient(165deg, #0F6B45, #0A3A28)";
