version: '3.9'

services:
  postgres:
    image: postgres:17-alpine
    container_name: pi-financeiro-db
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: pi_financeiro_dev_secret
      POSTGRES_DB: pi_financeiro
      PGDATA: /var/lib/postgresql/data/pgdata
      # DATABASE_URL for pg-boss (cron jobs) - constructed from other vars
      DATABASE_URL: postgresql://postgres:pi_financeiro_dev_secret@postgres:5433/pi_financeiro
    ports:
      - "5433:5432"  # Use 5433 since 5432 is taken
    volumes:
      - postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U postgres -d pi_financeiro"]
      interval: 5s
      timeout: 5s
      retries: 5

  # Future: Cron worker container for pg-boss jobs
  # Uncomment when ready to deploy cron workers separately
  # cron-worker:
  #   build:
  #     context: .
  #     dockerfile: Dockerfile.jobs
  #   container_name: pi-financeiro-cron
  #   environment:
  #     - DATABASE_URL=postgresql://postgres:pi_financeiro_dev_secret@postgres:5433/pi_financeiro
  #     - NODE_ENV=production
  #   depends_on:
  #     - postgres

  # Evolution API for WhatsApp (placeholder for future)
  # evolution-api:
  #   image: atendai/evolution-api:latest
  #   container_name: pi-financeiro-whatsapp
  #   environment:
  #     - SERVER_URL=${SERVER_URL:-http://localhost:8080}
  #     - AUTHENTICATION_API_KEY=${EVOLUTION_API_KEY}
  #     - DATABASE_PROVIDER=postgresql
  #     - DATABASE_CONNECTION_URI=postgresql://pi_financeiro:pi_financeiro_dev_secret@postgres:5432/pi_financeiro
  #   ports:
  #     - "8080:8080"
  #   depends_on:
  #     - postgres

volumes:
  postgres_data:
    driver: local

networks:
  default:
    name: pi-financeiro-network