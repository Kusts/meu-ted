import { Type } from "typebox";
import { capabilityDisabled } from "./api-tool-helpers.js";
export const deleteNotification = { name: "delete_notification", description: "Remove uma notificação.", parameters: Type.Object({ householdId: Type.Optional(Type.String()), settingId: Type.Optional(Type.String()), chatId: Type.Optional(Type.String()), notificationType: Type.Optional(Type.String()) }), execute: async () => capabilityDisabled("delete_notification") ?? { success: false, reason: "Capability delete_notification has no DELETE API route yet" } };
export const processNotifications = { name: "process_notifications", description: "Processa notificações pendentes.", parameters: Type.Object({ householdId: Type.Optional(Type.String()), dryRun: Type.Optional(Type.Boolean()), grouped: Type.Optional(Type.Boolean()) }), execute: async () => capabilityDisabled("process_notifications") ?? { success: false, reason: "Capability process_notifications has no API route yet" } };
export const getNotificationLog = { name: "get_notification_log", description: "Lista histórico de notificações.", parameters: Type.Object({ householdId: Type.Optional(Type.String()), limit: Type.Optional(Type.Integer()), notificationType: Type.Optional(Type.String()) }), execute: async () => capabilityDisabled("get_notification_log") ?? { success: false, reason: "Capability get_notification_log has no API route yet" } };
export const testNotification = { name: "test_notification", description: "Gera preview de uma notificação.", parameters: Type.Object({ householdId: Type.Optional(Type.String()), notificationType: Type.String(), thresholdDays: Type.Optional(Type.Integer()) }), execute: async () => capabilityDisabled("test_notification") ?? { success: false, reason: "Capability test_notification has no API route yet" } };
