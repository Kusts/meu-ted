# Release V049 API — 2026-09-08 (3cf4cba)

Deploy da migração legado-segura V049 (category uniqueness) + docs de
release. Pré-checagem abortou o deploy próprio: a VPS já estava em
`3cf4cba` (deploy paralelo anterior, ~2026-09-08 21:56 UTC) — nada a fazer.

## Commits (9a7e988..3cf4cba)

- `e09ce26` docs(release): estado real do rename Meu Ted e registro do release 2026-09-07.
- `3cf4cba` fix(migrations): legacy-safe category uniqueness (V049).

## Pré-checagem (task §1)

- Working tree local: limpo de mudanças TRACKED (só o NÃO rastreado
  `apps/api/e2e-seed.tmp.ts`, insumo de outra task — não tocado).
- HEAD local = `3cf4cba` (`git rev-parse HEAD` + `git log --oneline -5`).
- SSH OK (`deploy@187.77.249.47`, chave `id_ed25519_hostinger_vps`).
- Release ATUAL na VPS já = `3cf4cba` → deploy próprio NÃO executado.

## API — VPS Hostinger (deploy@187.77.249.47, ~/infra/pi-finance-api)

Mecanismo padrão NÃO re-executado (backup → sync → build → tag → compose
up -d já havia sido aplicado por processo paralelo).

- Release: `3cf4cba`; anterior `9a7e988` preservado (`app-pre-3cf4cba/.release`
  = `9a7e988`, `app/.release` = `3cf4cba`, tarball `app-release-3cf4cba.tgz`
  2026-09-08 21:55:57 UTC).
- Imagens: `pi-finance-api:main` == `pi-finance-api:release-3cf4cba`
  (`bb17179dfbd3`); `release-9a7e988` (`a304e6994e65`) e
  `backup-before-b898d6a-20260907T182834Z` retidos.
- Container `pi-finance-api`: `Up 2 hours (healthy)`, `RestartCount=0`,
  `StartedAt=2026-09-08T21:56:30Z`, sem restart loop.
- Boot DB_SCHEMA=legacy: `legacyMigrations [49]` aplicado.
- V049 é legacy-safe: `LEGACY_SAFE_PREFIXES` em
  `apps/api/src/read-models/sql/migrate.ts` inclui `V049` (DO block
  dual-schema: no-op no canônico onde V048 já rodou; dedupe + índice
  único parcial no legado); V048 deliberadamente fora (exige
  `categories.status`, ver `LEGACY_EXCLUDED_JUSTIFICATIONS`).
- 7 WARNs `baseline_drift` esperados (V3, V8, V9, V10, V11, V12, V40 —
  histórico pré-guard, boot continua).
- Rollback disponível: imagens `pi-finance-api:release-3cf4cba` (bb17179dfbd3)
  e `pi-finance-api:main` (same SHA); backups de diretório em
  `app-bak-0cff771-20260906T125128Z`, `app-bak-11172e0-20260906T155130Z`,
  `app-bak-11172e0-20260907T182834Z`, `app-bak-20260822T160839Z`,
  `app-bak-499d2cf-20260906T103037Z`, `app-bak-9380232-20260906T112743Z`,
  `app-bak-d0f6908-20260906T135211Z`, `app-bak-swap-11172e0-20260907T182834Z`.
  Mechanism: `docker tag <backup-image> pi-finance-api:main && docker
  compose up -d` (ou `docker tag <backup-image> pi-finance-api:release-3cf4cba
  && docker compose up -d`). Tarballs de referência: `app-release-3cf4cba.tgz`
  (2026-09-08 21:55:57 UTC), `app-release-9a7e988.tgz`, `app-release-b898d6a.tgz`.
  Diretório `app-pre-3cf4cba` contém o estado anterior (`9a7e988`, `.release` =
  `9a7e988`).

## Saúde / validação (sem deploy próprio)

- `GET https://api.synkroo.com.br/health` → 200 `{"status":"ok"}` (externo).
- Healthcheck interno do container: 200 contínuo (logs `request completed`).
- Smoke superfície pública: `POST /api/auth/sign-in/email` sem credencial →
  404 JSON (`Route POST:/api/auth/sign-in/email not found`) — 4xx-JSON
  aceitável, sem 5xx.
- Gates de código NÃO re-executados (nenhum código de produto tocado;
  só docs nesta task).

## Cloudflare

Nenhum deploy Cloudflare (PWA/Agent já atuais, fora do escopo da spec).
Nenhum rename de worker/serviço/diretório.
