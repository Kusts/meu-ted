# Resumo — Itens 2 e 3 (sessão Orca, agy = coder)

**Data:** 2026-08-19 · **Run:** `run_842c8882bc4c`
**Coordenador/planner:** Pi · **Coder:** agy (Antigravity CLI)

---

## Item 2 — Consumo de GET /dashboard/summary na UI ✅ (commit `674db50`)

O PWA passou a consumir os agregados monetários **server-owned** do endpoint
`GET /dashboard/summary` em vez de fabricá-los client-side.

| Mudança | Detalhe |
|---|---|
| `app-state-context.tsx` | Adicionado `dashboardSummary` (state) + `refreshDashboardSummary()`; busca via `fetchDashboardSummary` no boot |
| `HomePage.tsx` | Usa `dashboardSummaryGate({ serverSummary })` — se true, renderiza `totalBalanceCents`/`monthIncomeCents`/`monthExpenseCents`/`monthNetCents` do servidor; se false, mostra placeholder `—` (não fabrica a partir de dados locais parciais) |
| `mock-data.ts` | Adicionado `mockDashboardSummary` p/ modo mock |
| Testes | 4 novos casos G5.2.9: server-owned renderiza, gate false não fabrica, `aggregateFromSnapshot` retorna null p/ snapshot parcial, refresh no load |

**Verificação:** `pnpm test:pwa --run` → **87/87 arquivos, 1039/1039 testes, 0 falhas**.

## Item 3 — Correção do LEGACY_SAFE_PREFIXES ✅ (commit `257ca42`)

| Mudança | Detalhe |
|---|---|
| `migrate.ts` | `LEGACY_SAFE_PREFIXES` enxugado para `['V003','V008','V009','V010','V011','V012']` — apenas migrações financeiras aditivas reais legacy-safe |
| Rationale | V013-V030 dependem de schema moderno (Better Auth, workspaces, ownership transfers, `pending_operations`, push, adoption) — sobre schema legado falhavam com `relation does not exist` (V022), `constraint already exists` (V015), `column does not exist` (V023) |
| Testes de manifest | 6 arquivos ajustados: migrações modernas (V017-V025) passaram a ser validadas no manifest **canônico** (`expectedMigrationManifest()`), não no legacy — sem enfraquecimento |
| Escalação | agy escalou via `orca orchestration question` se podia ajustar os testes; coordenador **autorizou** (a enxugada é correta) |

**Verificação:** suíte unitária da API **106/106 arquivos, 731/731 testes verdes**; PWA 87/1039 verdes. O teste de integração `identity-workspace-migration` (2 bancos, `DATABASE_URL_TEST_LEGACY`) permanece gated por infra (timeout de hook 10s) — pré-existente, não regressão.

## Observações

- Orca: dispatch sequencial (1 ativo/terminal) — Item 2 → Item 3.
- `identity-workspace-migration.test.ts`: timeout de hook de 10s ao migrar 2 bancos; pré-existente (requer ambientes especiais), não relacionado à enxugada.
- WIP pré-existente (`apps/api/src/writes/postgres.ts`, e2e, docs) não foi tocado.
