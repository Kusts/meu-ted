# V4.1 Publication Readiness (2026-09-16)

Pré-auditoria read-only para tornar o repositório publicável (SPEC Fase 6 / plano Phase 6). **Nenhuma ação irreversível foi executada** — sem rotação, sem rewrite, sem mudança de visibilidade.

## 1. Scans executados

- **Working tree:** `rg` (todos os arquivos trackeados) com padrões de private keys, AWS/GitHub/OpenAI/Slack/Google/npm tokens, JWTs, URL-embedded credentials, VAPID, SMTP.
- **Filenames ever committed:** `git log --all --diff-filter=A --name-only` (filtrado).
- **Histórico:** `git log --all -S/-G` para 18 padrões de segredo + `git grep` nos heads.
- **Rastreabilidade de refs:** `git tag --contains` / `git branch -a --contains` / `git merge-base --is-ancestor` para cada achado.
- **Scanner local:** `scripts/security-secrets.mjs` (gitleaks skipped on win32 — CI Linux é autoritativo).

## 2. Resultado — segredos

### Árvore trackeada (o que seria publicado do HEAD)
**0 segredos reais.** Achados = falsos positivos revisados (fixtures de redaction, regexes de detecção, token público jwt.io, placeholders `test:test@`/`<senha>`/`change-me`) — todos agora cobertos pelo gate `pnpm public-safety` (PASS em modo default).

### Histórico alcançável a partir de refs publicáveis (main + tags)
**Nenhum segredo detectado** nos sweeps `-S`/`-G`. O commit `f7488d0` (feature SMTP) refere `SMTP_PASS` apenas como **nome de variável** em código — seguro.

### Objetos LOCAIS inalcançáveis (NÃO seriam publicados por push)
- Commit de snapshot `11b82e8` contém **`.env.e2e.creds`** com credenciais e2e reais (`E2E_ADMIN_EMAIL` = Gmail pessoal, senha de 8 chars; guest idem). **Não alcançável de nenhuma branch/tag** — pendurado em objetos locais (via stash/reflog). Risco de publicação: **nenhum via push normal**; risco local: baixo, mas real.
- Commit `d926265` contém dump de estado de UI (`TEMPhostinger-restore.json`) — **falso positivo** (metadata de janela/screenshot, sem credenciais).
- Screenshots `.claude/audit/*.png` em snapshots — apenas objetos locais.
- **Sugestão (não-executada):** `git stash drop` + `git gc --prune=now` para eliminar os objetos; rotação opcional das senhas e2e (elas autenticam contas reais em produção).

## 3. Resultado — metadados pessoais/operacionais (BLOQUEIA publicação)

`pnpm public-safety` (modo default) lista **79 warnings**; `--strict` falha. Grupos:

| Grupo | Exemplos | Onde | Ação proposta |
|---|---|---|---|
| IP de produção + usuário SSH + nome da chave | `187.77.249.47`, `deploy@`, `id_ed25519_hostinger_vps` | `AGENTS.md`, `docs/ESTADO-…`, `docker/pi-stack/README.md`, `scripts/*.bat`, ~10 docs de release | Parametrizar/remover de docs e scripts |
| Hostname interno Evolution | `evo.synkroo.com.br` | `docker/pi-stack/`, docs | Parametrizar |
| E-mails pessoais reais | `walissonead@gmail.com` (admin default + tests), `synkrooia@gmail.com`, `convidado.teste.pi@gmail.com` | `apps/api/src/env.ts`, `routes/index.ts` (defaults), tests, docs | Trocar defaults por placeholder seguro + env obrigatória em prod |
| Namespace pessoal Cloudflare | `pi-finance-{pwa,agent}.walissonead.workers.dev` | código (CSP/origins), workflows, tests, docs | Mover para env/config no momento da publicação |
| PII em fixture | CPF válido `529.982.247-25` (`dlp-persistence.test.ts`) | tests | Trocar por inválido óbvio |

Não há dados de produção/dumps reais trackeados: `scripts/anonymized-dump.sql` é sintético (header + amostra verificados); `data/`/backups não são trackeados; `.env*` reais não são trackeados (só `.example`).

## 4. Gate `public-safety` (implementado nesta sessão)

- `scripts/check-public-safety.mjs` + `pnpm public-safety` + job `public-safety` no `ci.yml` (incluído no `gate`).
- ERROR (falha sempre): private keys, tokens, credenciais em URL (exceto placeholders allowlisted com motivo), `.env`/`.dev.vars` trackeados, dumps/backups/keyfiles.
- WARN (falha só com `--strict`): metadados proibidos (IP, SSH, e-mails pessoais, hostnames internos).
- Estado atual: **PASS default / FAIL strict** — é exatamente o sinal "pronto para publicar": publicar exige `--strict` verde.

## 5. Classificação dos workflows (CI público)

| Workflow | Classe |
|---|---|
| `ci.yml` (13 jobs: api, agent, broker, pwa, pwa-cloudflare, postgres, docker, capabilities, write-policy, governance, security, docs, public-safety, gate) | **SAFE_FOR_UNTRUSTED_PUBLIC_CI** — zero secrets; Postgres efêmero (`postgres:16-alpine`, dados sintéticos); testes não exigem segredo (VAPID fake nos fixtures; evals determinísticos) |
| `pwa-ci.yml` (quality, e2e) | **SAFE_FOR_UNTRUSTED_PUBLIC_CI** — zero secrets, fixture local, artifacts só de reports |
| `agent-deploy`/`pwa-deploy` (gate) | TRUSTED_CI (`actions: read`) |
| `agent-deploy`/`pwa-deploy` (smoke) | TRUSTED_CI (GETs públicos fail-closed) |
| `agent-deploy`/`pwa-deploy` (deploy) | **PRODUCTION_DEPLOY** — `CLOUDFLARE_API_TOKEN`/`ACCOUNT_ID`; sem `environment:`/reviewers (única proteção = gate CI-por-SHA) — endurecer antes de publicar |
| `production-smoke.yml` | TRUSTED_CI (dispatch manual, Environment `production-smoke`) |

Fork-PRs: seguros — nenhum job de `pull_request` toca secrets; deploys só via `workflow_run` em `main`.

## 6. Veredito

**PUBLICATION READINESS = NOT READY** (como esperado pré-sanitização).

Caminho para READY (nenhuma ação executada ainda — todas sob gate humano):
1. Sanitizar os 79 warns (parametrizar hosts/e-mails, defaults de admin por env obrigatória, fixture CPF) — sem rewrite de histórico (tudo está no HEAD).
2. `git stash drop` + GC (objetos locais com `.env.e2e.creds`) + rotação opcional das senhas e2e.
3. `pnpm public-safety --strict` verde.
4. Endurecer deploys: `environment: production` com reviewers + `permissions: contents: read` nos workflows de PR.
5. Autorização humana → mudança de visibilidade → CI remoto verde em janela definida (re-privatizar se vermelho).
