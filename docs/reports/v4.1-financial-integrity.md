# V4.1 Financial Integrity â€” Phase 2 + canonical 4 (2026-09-17)

Phase 2 do plano (Â§6, Tasks 2.0â€“2.19; gates D1/D3/D4 em `v4.1-decision-gates.md`) + itens canÃ´nicos da Phase 4 (Tasks 4.1â€“4.11). DecisÃµes: D1=B proibir negativo, D3=sempre-expense, D4=sucessor mantido + `paidTransactionId` obrigatÃ³rio, D2=ledger `transactions`.

## 1. Payables (Tasks 2.1â€“2.3, D3/D4)

| Fix | EvidÃªncia |
|---|---|
| Lock `FOR UPDATE` + validaÃ§Ã£o pÃ³s-lock (canÃ´nico e legacy) | `payables/postgres.ts:59`, `payables/legacy-postgres.ts:262` |
| Pagamento sempre cria expense; escape `createTransaction:false` rejeitado | `routes/payables.ts:57-64` (schema `.strict()` + comentÃ¡rio D3) |
| DÃ©bito de saldo via primitiva de ledger no pagamento | `payables/postgres.ts:75` (lock account), `:107` update paid+link |
| Unpay exige `paidTransactionId` (422 sem, 409 mismatch) | `routes/payables.ts:66-73`, stores `:600`, `legacy-postgres.ts:351-362` |
| Guarda de status anti-duplo-pagamento | `payables/postgres.ts:59-107`, `legacy-postgres.ts:229-311` |

## 2. Cards / statements / goals (Tasks 2.4â€“2.11)

- Ledger authority D2: `updatePurchase` sincroniza purchase+transaction em tx Ãºnica com locks (`cards/legacy-postgres.ts:728-787`; canÃ´nico `cards/postgres.ts:673`).
- Placeholders dinâmicos (Tasks 2.5/2.6/2.19): legacy `cards/legacy-postgres.ts` usa locks e queries parametrizadas nas faixas `:355-370,419-447,739-787`; subscriptions PATCH dinâmico — builder `sets.push` em `subscriptions/legacy-postgres.ts:127-131` (commit `a1a5045`).
- Statement total com lock + `SUM` recomposto (`cards/legacy-postgres.ts:156-171`, `cards/postgres.ts:81-111`); `payStatement` com lock de account (`cards/postgres.ts:397`) e de statement.
- Goals atômicos (sem read-modify-write) — `SET current_amount_cents = current_amount_cents + $3` em `goals/postgres.ts:41-42` (commit `312d4bb`).
- PATCH transação: contrato estrito 422 — `.strict()` em `writes/types.ts:77,143` (commit `a1bbbcd`).
- Categories resolver central (2.14–2.15): budgets ganharam twin legacy (`server/index.ts:154-155`); consumers migrated via commits `45ef78d`+`124c6f9` (resolver em writes/cards/payables/budgets).
- Billing-month helper (2.16–2.17): `tests/unit/billing-month.test.ts` (19 testes); recurring validation (2.18, D9 OFF): `tests/routes/cards-v41-hardening.test.ts` + `postgres-cards-v41-hardening.test.ts`.

## 3. CanÃ´nico D1 + delta engine (Tasks 4.1â€“4.11)

- Clamps `GREATEST(0,â€¦)` removidos; validaÃ§Ã£o de saldo insuficiente sob lock (`writes/postgres.ts:131-146`, comentÃ¡rio `:522`).
- Delta engine reverse/apply (`writes/postgres.ts:501-545,667-696`); lock ordenado determinÃ­stico em transfers.
- `template_id` resolvido (`postgres-canonical-parity-v41.test.ts:18-19`); paid payable protegido contra delete (`writes/postgres.ts:89-103`).

## 4. Review rounds (9+3+3, relatados pelo solicitante)

Contagens e achados individuais: 3 rodadas de review (9 findings Phase 1-2 → commit `410888d`; 3 findings Phase 3 → commits `6ce2045`+parte de `410888d`; verificação round 2 → `6ce2045`). Série completa: 24 commits `dd10e2b..HEAD` (ver `v4.1-final-report.md` §3); cada finding cruza com o teste citado nas seções acima.

## 5. Provas de concorrÃªncia

- `tests/routes/payable-concurrency.test.ts`, `tests/routes/goals-concurrency.test.ts`, `tests/integration/postgres-canonical-parity-v41.test.ts:14-19` (transfer sem deadlock, 10Ã—â†’soma exata). Totais de execuÃ§Ã£o: rodar `pnpm --filter meu-ted-api test:concurrency` e registrar.

