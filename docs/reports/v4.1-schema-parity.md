# V4.1 Schema Parity — Phase 10 §14.4 (2026-09-17)

Comparação consolidada legacy × canonical sobre `v4.1-hardening` (produção = legacy, `DB_SCHEMA=legacy`; `server/index.ts:76,141-155`).

| Superfície | Legacy | Canonical | Status pós-V4.1 |
|---|---|---|---|
| Wiring | adapters `createLegacy*` (`server/index.ts:8-47,141-155`; `production-routes.ts:6-23,116-184`) | adapters canônicos no branch não-legacy | Ambos vivos; troca só por `DB_SCHEMA` + gate humano |
| Writes (expense/income/transfer/PATCH/DELETE) | `legacy-postgres.ts:128-139,289-361` (paid-link guard, join claim-tx) | `postgres.ts:89-146,501-696` (locks, sem clamp) | Pares endurecidos; divergência D1 intencional |
| Payables (pay/unpay) | `FOR UPDATE` + guarda status (`legacy-postgres.ts:262-311`) | `FOR UPDATE` + débito de saldo (`postgres.ts:59-107,600-640`) | Paridade pós-fase 2/4 |
| Statements | lock + `SUM` (`legacy-postgres.ts:156-171,355-447`) | lock + `SUM` (`postgres.ts:81-111,397`) | Pares |
| Cards ledger | sync purchase+tx em tx (`legacy-postgres.ts:728-787`) | idem (`postgres.ts:673`) | Pares (D2) |
| Categories | resolver compartilhado; budgets twin legacy (`server/index.ts:154-155`) | mesmo resolver | Unificado (Task 2.15) |
| Budgets | twin legacy criado | `budgets/postgres.ts` | Pares |
| Saldo | computado (sem clamp) | materializado + `CHECK >= 0` + validação (D1=B) | **Divergência documentada** (Part C da paridade) |
| Statement coverage (recon) | 12 checks | 11 checks (**sem `statement_payment_coverage`**, `sql.ts:24-39`) | **Gap aberto** (ver `v4.1-canonical-parity.md` §4) |
| Idempotência | V2 + compat (`idempotency.ts:96-124`) | idem | Pares |

## Divergências conhecidas restantes

1. D1 over-balance (legacy aceita, canônico 400) — severidade: esperada/bloqueadora de migração de histórico; dono: decisão D1 + reconciliação Fase 10.
2. `statement_payment_coverage` ausente no canônico — severidade média; dono: Fase 4 follow-up.
3. Semânticas D5–D9 (budgets anchor, rollover, subscriptions weekly/annual, recurring OFF) — formais, sem mudança de comportamento; dono: releases futuras.
