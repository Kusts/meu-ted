# V4.1 Baseline (Tasks 0.3–0.5, 2026-09-17)

Fotografia do estado pré-implementação sobre a branch `v4.1-hardening` @ `27b3288` (base de planejamento `dd10e2b`).

## Configuração de execução

- Local: Windows, Docker Desktop ativo, Postgres de teste `pi-v4-test-pg` (postgres:16-alpine, porta 15432, db `pi_test`, marker `_test_marker` = `00000000-…-d00d`), banco **recriado do zero** nesta sessão (estado stale de sessão anterior causava falha de migration V048).
- Env de testes PG: `DATABASE_URL_TEST=postgresql://…@127.0.0.1:15432/pi_test` + `DB_TEST_MARKER` (não persistidos em escopo usuário/máquina).
- Produção (referência): API `v3-3305152`, `DB_SCHEMA=legacy`, Postgres 15.18, migrations V052 (ver `v4.1-runtime-inventory.md`).

## Baseline de testes (Task 0.3)

| Suíte | Comando | Resultado |
|---|---|---|
| Typecheck root | `pnpm typecheck` | PASS (4 pacotes) |
| Lint | `pnpm lint` | api 135 warnings `noExplicitAny` pré-existentes (0 erros); agent 4 warnings; broker OK; **PWA/eslint: timeout >11min local (não concluído)** |
| API unit | `pnpm --filter meu-ted-api test` | **1659 passed / 14 skipped / 0 failed** (skips PG-gated: XLT-07 ×8, XLT-10 ×5, device-rotation ×1) |
| API full c/ PG (`test:all`) | idem + env | **1773 passed / 10 failed / 18 skipped** — falhas pré-existentes em suites integration dormentes (fora do CI atual): FK de seed vs migrations (users_auth_user_id_fkey, households_shared_owner_chk, agent_llm_models_provider_id_fkey), drift de checksum (suite própria de drift baseline), ordem/polução entre arquivos que compartilham o banco |
| PWA unit | `pnpm --filter pwa test` | 1789 passed / 1 failed — **flaky confirmado** (`AuditPage › limpar filtros`; passa isolado 3/3) |
| Agent | `pnpm --filter pi-finance-agent test` | 468 passed / 1 skipped (ted-v3 real-model, esperado) |
| llm-contracts | build + test | 23/23 passed |
| docs:lint | `pnpm docs:lint` | PASS (0 issues) |
| governance:check | `pnpm governance:check` | PASS |
| public-safety | `pnpm public-safety` | PASS não-strict; **85 warnings** (deriva +6 vs 79 documentados em AGENTS.md) |
| API build | `pnpm --filter meu-ted-api build` | PASS |

## Reconciliation (Tasks 0.4)

Implementado em `apps/api/src/scripts/reconciliation/` (detectors puros + SQL por layout legacy/canonical + CLI `pnpm --filter meu-ted-api reconciliation`, SELECT-only com `default_transaction_read_only=on`). 72 testes de detectores/CLI passando. Execução contra banco real validada em `pi_test` vazio (SQL correto; 0 linhas). Execução contra produção fica para a Fase 10 (requer túnel SSH e gate humano de acesso).

Limitações conhecidas: saldo canônico sem âncora de saldo inicial (`info/unanchored_basis`); statement-payment-coverage canônico omitido (pagamento de fatura canônico não cria transaction — defeito da Fase 4); legacy casa pagamento de fatura por descrição `Pagamento fatura {cycle}` (edição manual gera finding para revisão humana).

## Reconciliação de dados reais

NÃO executada nesta fase (produção inacessível sem autorização de acesso; banco local sem dados). A infraestrutura está pronta para rodar na Fase 10 sobre dump/túnel autorizado.

## Drift e débitos registrados

- public-safety: 79 → 85 warnings (investigar na Fase 6).
- Lint PWA local excede 11 min (investigar cache/particionamento na Fase 9).
- Flaky PWA `AuditPage` (não bloqueante; monitorar).
- Suites integration dormentes quebradas (FK seeds, pollution) — escopo da Task 9.1.
- `_migrations` local de teste agora limpa (V001→V054 aplicadas do zero).

## Veredito

Baseline válida para iniciar Phase 1. Runtime confirmado Legacy. Nenhum bloqueador P0.
