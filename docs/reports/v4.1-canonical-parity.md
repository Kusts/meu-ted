# V4.1 Canonical Parity + Shadow Reads — Phase 4 / §17.3 (2026-09-17)

Phase 4 do plano (§8, Tasks 4.1–4.13; sem ativar Canonical) + shadow reads (plano §14.6, SPEC §17.3).

## 1. Resolução D1 (Task 4.1)

D1 = opção B (proibir negativo; `v4.1-decision-gates.md:9`). `CHECK (balance_cents >= 0)` mantido; clamps removidos (`writes/postgres.ts:131-146,522`).

## 2. Suite de paridade (Task 4.13)

- **Part A (sempre roda):** sequência canônica in-memory com invariante saldo==ledger (`tests/contract/legacy-canonical-parity.test.ts:7-17,37-54`).
- **Part B (PG-gated):** Postgres canônico × legacy em schemas isolados, equivalência happy-path (`:9-10`).
- **Part C (divergência documentada D1):** over-balance expense → canônico 400, legacy contabiliza e negativiza; assert como diferença-esperada (`:11-16`).
- Provas PG Tasks 4.2–4.11 em `tests/integration/postgres-canonical-parity-v41.test.ts:9-19` (clamp/money-creation RED→GREEN, matriz delta, transfers sem deadlock, pay/unpay com saldo exato, `template_id`).

## 3. Shadow reads (§14.6 / §17.3)

Shadow reads executados = Parts B/C acima (leituras comparativas legacy × canonical sem usar Canonical em produção). **Shadow reads sobre dados REAIS de produção NÃO executados** — atividade de cutover (requer túnel SSH + gate humano §3.4).

## 4. Gap `statement_payment_coverage` canônico — STATUS ATUAL

**Gap persiste no SQL de reconciliação:** `CHECKS_BY_LAYOUT.canonical` (`scripts/reconciliation/sql.ts:24-39`) **omite** `statement_payment_coverage` (só legacy tem os 12 checks; canônico tem 11). O detector (`:139-157`) casa pagamento por descrição `Pagamento fatura {cycle}` — que AGORA também se aplica ao canônico: `payStatement` canônico cria a expense `Pagamento fatura ${cycle}` (`cards/postgres.ts:417`, Phase 2), logo a cobertura pode ser portada ao SQL canônico como follow-up direto da Fase 10 de cutover (requer reindexar os checks por layout).

## 5. Migration dry-run (§14.7, ponte)

Pré-condição para `blockers = 0` (§20). Dry-run sobre cópia sanitizada/sintética **não executado** nesta fase; base disponível: `scripts/anonymized-dump.sql`, `scripts/rehearse-migration.mjs:32,91`, validação `scripts/migration-validate.mjs` (VAL.18). Registrar comando e saída quando executado.
