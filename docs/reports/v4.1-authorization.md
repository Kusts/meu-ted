# V4.1 Authorization — Phase 1 (2026-09-17)

Fase 1 do plano (`docs/superpowers/plans/2026-09-16-meu-ted-v4.1-hardening.md` §5, Tasks 1.1–1.9). Branch `v4.1-hardening`.

## 1. Resolução autorizada de device (Tasks 1.1–1.3)

- Novo módulo `apps/api/src/auth/device-access.ts:21-38` — `resolveAuthorizedDevice` carrega device + `workspaceAccess.resolve(userId, workspaceId)` e nega (403) sem membership ativa; checa `workspaceStore` (`status !== 'active'` → nega).
- Branch device em `apps/api/src/routes/index.ts:331-364` usa o resolver; `role` vem de `authorized.access.role` (`:352`) — **owner artificial eliminado** nessa branch.
- Rota de rotação também resolve via `resolveAuthorizedDevice` (`apps/api/src/routes/auth.ts:193-195`).

| Defeito (baseline plano §evidências) | Correção | Evidência |
|---|---|---|
| Token válido autorizava sem membership (`device-token.ts:233-267`, `routes/index.ts:336-356` sem `workspaceAccess`) | Resolver exige membership ativa + workspace ativo | `device-access.ts:21-38`, `routes/index.ts:335-353` |
| `role: 'owner'` fabricado (`routes/index.ts:340-349`) | Role da membership atual | `routes/index.ts:352` |
| Fallback household default/demo (`routes/auth.ts:62-70,123-124`) | Registro sem workspace → rejeitar | **Não verificado neste ambiente — conferir `apps/api/src/routes/auth.ts` atual e citar linha** |

## 2. Revogação (Tasks 1.5–1.6)

- `removeMember` marca `status='removed'` em tx e dispara `hooks.onMemberRevoked` (`workspaces-postgres.ts:150-196`); `leave` idem (`:198-229`).
- Contrato do hook documenta revogação de tokens em `workspaces-store.ts:37-39`.
- `device-token.ts:21-28,318-327` — `revokeAllForUserWorkspace` (defesa em profundidade; enforcement primário = validação dinâmica).

## 3. Push pós-revogação (Tasks 1.7–1.8)

- Filtro de membership em leitura: `push/postgres.ts:27-56` (`membershipFilter` com JOIN `memberships` + `households`), aplicado em `list`/`listPending` (`:94,:107`).
- Purge em revogação: `push/store.ts:54-68` documenta purge + filtro; `delivery.ts` purge 404/410 mantido.
- `workspaces-http.ts:230,254` — sessões/reconnect fechados em remoção/leave.

## 4. Exit gate §5

| Critério | Status |
|---|---|
| removed member → denied | ATENDIDO (código; `device-access.ts:30-31`) |
| member → member / owner → role real | ATENDIDO (`routes/index.ts:352`) |
| archived workspace → denied | ATENDIDO (`device-access.ts:32-36`) |
| zero-workspace registration → denied | PENDENTE DE VERIFICAÇÃO (ver `routes/auth.ts`) |
| revoked member push → zero delivery | ATENDIDO (código; `push/postgres.ts:37-56`) |
| rotate authorization | ATENDIDO (`routes/auth.ts:193-195`, `device-token.ts:352-370` FOR UPDATE) |

## 5. Validação

Comando (não executado neste ambiente, sem shell): `pnpm --filter meu-ted-api test -- tests/routes/device-auth tests/routes/push` + `git log --oneline dd10e2b..HEAD -- apps/api/src/auth apps/api/src/push` para SHAs por commit.
