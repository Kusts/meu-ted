# V4.1 Runtime Inventory (2026-09-16)

Levantamento read-only do runtime real. Método: sondas SSH read-only na VPS (docker inspect/printenv não-sensível/psql SELECT), sondas HTTPS públicas e inspeção do repositório (`main@dd10e2b`).

## Produção (VPS Hostinger `srv1773156`)

| Item | Estado confirmado | Evidência |
|---|---|---|
| API | container `pi-finance-api`, imagem `pi-finance-api:main` = tag `v3-3305152`, criada 2026-09-15T21:20Z, healthy 28h | `docker ps` + `docker image inspect` |
| API env (não-sensível) | `DB_SCHEMA=legacy`, `NODE_ENV=production` | `printenv` (apenas variáveis não-sensíveis) |
| Node da API | v22.23.2 | `node -v` no container |
| Postgres do app | **15.18** (Alpine) no container `evolution-postgres`, database `pi_financeiro` | `select version()` + host extraído de `DATABASE_URL` sem imprimir credenciais |
| Migrations | `_migrations` top = **V052** (`pending_operation_execution_recovery`), V051, V050, V049, V047 — estado exato V3 | `psql -tAc "select version,name from _migrations ..."` |
| API HTTP | `/health` → `{"status":"ok"}`; `/ready` → `{"status":"ready"}` (sem exposição de SHA — release identity é Task 9.9 futura) | `curl https://api.synkroo.com.br` |
| PWA | **V3 confirmada por assinatura de headers**: `Permissions-Policy: microphone=()` (defeito corrigido na V4) e CSP `connect-src 'self' https://api.synkroo.com.br https://pi-finance-agent…` (egress direto pré-V4) | `curl -I https://pi-finance-pwa.walissonead.workers.dev/` |
| Agent | `/health` → `{"status":"ready","schemaVersion":5}`; último deploy documentado `0e557bba` @ `3305152` | `curl https://pi-finance-agent.walissonead.workers.dev/health` + `AGENTS.md` |
| Codex Broker | **NÃO encontrado em produção** (ausente do `docker ps` da VPS) — runtime externo/desconhecido | `docker ps` |
| Outros containers | `synkroo-prod-postgres` (pgvector:pg17 — **não** é o DB do app), `painel-postgres` (pg16), `evolution-postgres` (pg15.18, hospeda `pi_financeiro`), `evolution-go`, traefik, cloudflared ×2, whatsapp-sidecar | `docker ps` |

`UNKNOWN` explícitos: SHA exato da PWA/Agent deployados (nenhum endpoint expõe gitSha hoje — confirmar via `wrangler`/Cloudflare API exigiria token; classificar por assinatura de headers cobre V3 vs V4).

## Repositório (2026-09-16)

| Item | Estado |
|---|---|
| Branch / HEAD | `main` @ `dd10e2b1a004edae5c4ec60c3cb6c993f675c586`, idêntico a `origin/main` (fetch feito) |
| Working tree | Limpo, exceto documentos desta sessão de readiness (untracked) |
| V4 | Mergeada em `ce787e4`; migrations V053/V054 **não aplicadas** em produção |
| Stash | 1 stash local (`pre-consolidation-merge`) — não é ref publicável |
| Tags | `pre-g6-legacy-retirement`, `rollback-pre-p3-2026-08-24` (rollback markers) |
| CI remoto | Bloqueado por billing (startup failure); deploys manuais wrangler |

## Deploy targets

- **API:** VPS Hostinger, compose `~/infra/pi-finance-api` (usuário `deploy`, acesso por chave SSH — ver `docs/ops/vps-access.md`).
- **PWA/Agent:** Cloudflare Workers via `wrangler deploy` manual (OAuth local).
- **Broker:** sem deploy de produção identificado.
