# V4.1 Pre-implementation Readiness (2026-09-16)

Sessão de readiness pré-implementação. Nada da V4.1 foi implementado. Fontes: SPEC V4.1, plano 2026-09-16, revisão da spec/plano, AGENTS.md, estado real do repo, sondas read-only de produção.

## 1. Baseline

```text
Repository HEAD: main @ dd10e2b1a004edae5c4ec60c3cb6c993f675c586 (== origin/main, fetch feito)
Working tree: limpo, exceto documentos de readiness desta sessão (untracked)
SPEC baseline still valid: YES (nenhum commit desde dd10e2b; premissas re-verificadas no código)
Commits desde a auditoria: 0
```

## 2. Produção (sondas read-only de 2026-09-16)

```text
API: v3-3305152 (imagem 2026-09-15T21:20Z, healthy) | DB_SCHEMA=legacy | Node v22.23.2
PWA: V3 (confirmada por headers: microphone=(), CSP com egress direto)
Agent: /health ready (schemaVersion:5); último deploy documentado 0e557bba @3305152
Broker: sem deploy de produção identificado
DB: PostgreSQL 15.18 (container evolution-postgres, db pi_financeiro)
schema/migration version: _migrations top = V052 (estado exato V3)
confidence: HIGH (API/DB/migrations via SSH read-only; PWA via assinatura de headers; UNKNOWN: SHA exato PWA/Agent — nenhum endpoint expõe gitSha, Task 9.9)
```

Detalhes: `docs/reports/v4.1-runtime-inventory.md`.

## 3. Release gap

```text
Produção V3/V052 → main contém V4 (ce787e4, NÃO implantada; V053/V054 aditivas e compatíveis com binário V3) → V4.1 futura
```

Riscos: primeiro deploy carrega V3→V4(→V4.1) composto; quebra pontual new-PWA × old-API (`/client-events` 404); ordem obrigatória migrate→API→PWA. Bridge recomendado: **Opção A** (promover V4 primeiro, V4.1 depois) — decisão humana no gate §3.3. Detalhes: `docs/reports/v4.1-release-bridge.md`.

## 4. Documentação

```text
AGENTS.md: UPDATED (esta sessão — V4 mergeada, baseline dd10e2b, produção reconfirmada, V4.1 PRÉ-IMPLEMENTAÇÃO)
SPEC: CURRENT (baseline válida; nenhuma premissa invalidada)
PLAN: CURRENT + 1 correção documental aplicada (§10.6: dependência circular visibilidade↔CI resolvida — visibilidade exige public-safety --strict + autorização humana; CI remoto vira gate de confirmação pós-visualização)
```

## 5. Decisões D1–D11

Análise completa: `docs/reports/v4.1-domain-decisions.md`. Resumo: **D2 e D6 são ratificações** (o código já responde: ledger=transactions; rollover inexistente). **D1, D3, D4, D5, D7, D8, D9, D11 exigem decisão humana** (recomendações técnicas prontas); **D10 parcial**. **Nenhuma bloqueia Phase 0/1** — D1/D3/D4 bloqueiam partes da Phase 2 (gate Task 2.0); D9 bloqueia Fase 7; D11 bloqueia Release B.

## 6. CI

```text
Current blocker: billing/spending limit do GitHub Actions (externo ao projeto)
Public CI readiness: 100% dos jobs de teste são SAFE_FOR_UNTRUSTED_PUBLIC_CI (zero secrets; Postgres efêmero; dados sintéticos; testes não exigem segredo)
Production secrets isolated: SIM (só jobs deploy com CLOUDFLARE_API_TOKEN; sem environment protection — endurecer)
Remote CI path: (a) owner resolve billing → CI privado volta; ou (b) sanitizar → public-safety --strict → autorização humana → publicar → CI público gratuito → verde em janela de confirmação. Fork-público-como-runner: desnecessário (evidência: todos os jobs de teste rodam sem segredos).
```

Dependência circular resolvida documentalmente no plano §10.6 (ver §4 acima). Gate `public-safety` implementado e validado (PASS default / FAIL strict).

## 7. Publication safety

```text
Secret findings (árvore trackeada): 0 reais (4 FP revisados e allowlisted com motivo)
Historical findings: 0 em refs publicáveis; 1 arquivo de credenciais e2e REAL em objetos locais INALCANÇÁVEIS (11b82e8, via stash/reflog — não vai em push; sugerido stash drop + GC + rotação opcional)
Production data findings: nenhum trackeado (dump anônimo é sintético; data/ e .env* não trackeados)
Personal/operational metadata: 79 warnings mapeados (IP, SSH, e-mails pessoais, hostnames workers.dev, CPF fixture) — bloqueiam publicação, não bloqueiam implementação
History rewrite needed: NÃO (nenhum segredo em refs publicáveis)
Credential rotation needed: OPCIONAL (senhas e2e locais); nenhuma obrigatória para implementar
Publication readiness: NOT READY (caminho em docs/reports/v4.1-publication-readiness.md)
```

## 8. Blockers e stop conditions

```text
BLOCKER (impede iniciar V4.1): NENHUM
MUST RESOLVE BEFORE AFFECTED PHASE:
  - D1/D3/D4 (humano) antes de alterar payables/transfer da Phase 2 (gate Task 2.0)
  - D5–D8 antes de tocar semântica de budgets/subscriptions; D9 antes da Fase 7; D10/D11 antes da Phase 5
  - CI (billing OU publicação) antes da Phase 9; sanitização antes da mudança de visibilidade (Phase 6)
CAN PROCEED: Phase 0 (baseline/reconciliation), Phase 1 (authorization), Phase 3 (UoW/idempotency); Phase 2 EXCETO Tasks 2.1–2.3 (dependem do gate Task 2.0/D3/D4; Tasks 2.5–2.7 após ratificação D2); Phase 4 apenas tarefas independentes de D1 (4.6–4.7 locks, 4.11–4.13 schema debt/skips/parity) — Task 4.1 É a decisão D1 e Tasks 4.2–4.5/4.8–4.10 dependem dela
BLOCKED-UNTIL-GATE: Tasks 2.1–2.3 (D3/D4), 4.1–4.5 e 4.8–4.10 (D1/D3), semântica de budgets/subscriptions (D5–D8), Fase 7 recurring card (D9), Release B bearer (D11), switch de switch/offline (D10 parcial)
INFORMATIONAL: objetos locais com .env.e2e.creds (limpeza sugerida); stash pre-consolidation-merge; 1 UNKNOWN de versão PWA/Agent (sem endpoint de release identity)
```

## 9. Critérios §23 da solicitação

| Critério | Estado |
|---|---|
| repository baseline known | ✅ dd10e2b, working tree sob controle |
| production baseline known/bounded | ✅ V3-3305152/legacy/V052 confirmados por sondas |
| AGENTS.md current | ✅ atualizado nesta sessão |
| release gap understood | ✅ matriz + bridge + recomendação |
| early domain decisions resolved or gated | ✅ D2/D6 ratificáveis; restantes em gates explícitos (Task 2.0, §3.4) |
| no unknown P0 precondition | ✅ todas as premissas P0 confirmadas em código; novo achado (clamp em createExpenseInTx) registrado no plano |
| CI recovery path defined | ✅ duas rotas (billing / publicação), sem improvisos |
| publicization risks known | ✅ 79 warns mapeados, gate implementado |
| irreversible operations behind human approval | ✅ §3.4 do plano + gates desta sessão |
| SPEC and plan aligned with current code | ✅ (com a correção documental §10.6 registrada) |

## 10. Veredito

**READY TO IMPLEMENT V4.1** — iniciar pela Phase 0 (baseline/reconciliation) e Phase 1 (authorization lifecycle), respeitando os gates de decisão humana (Task 2.0, §3.3, §3.4) antes das fases dependentes.

Mudanças aplicadas nesta sessão (todas documentadas, nenhuma silenciosa): `AGENTS.md` atualizado; plano §10.6 corrigido; `scripts/check-public-safety.mjs` + job CI `public-safety` (infraestrutura preparatória da Fase 6, sem lógica V4.1); 5 relatórios de readiness.
