# V4.1 Idempotency — Phase 3 (2026-09-17)

Phase 3 do plano (§7, Tasks 3.1–3.11). Achado base: claim e efeito financeiro em txs independentes (`v4.1-mutation-inventory.md:5`).

## 1. Padrão single-tx keyed (Tasks 3.1–3.4)

- Produtores aceitam o client da tx do claim: tipo `IdempotencyProducer<T> = (tx?: unknown) => Promise<T>` (`writes/idempotency.ts:35` + comentário FIX-UNDO).
- Stores documentam o join: `writes/postgres.ts:501,545,667-696,1090`, `writes/legacy-postgres.ts:289,309,361`.
- Lifecycle claim→complete com status `processing|completed|failed` (`writes/idempotency.ts:37-59,138-237`); retry sobre `processing` órfão/`failed` reassume; payload divergente → `idempotency.conflict`.
- Postgres: `processing` INSERT + upgrade para `completed`, `resolveLostClaimRace` (`writes/postgres.ts:1156-1343`).

## 2. Cobertura (Task 3.11)

| Mutação | Single-tx keyed | Desvio aceito |
|---|---|---|
| Expense/income/transfer/PATCH/DELETE | Sim (produtores keyed) | — |
| Payable create/pay/unpay/PATCH/cancel | Sim | bulk (refresh/auto-create/from-template) REQ sem tx única — **não-transacional multilinha (débito conhecido)** |
| Card purchase/installments/cancel, statement pay, goals, budgets, subscriptions | Sim (keyed) | subscription create legacy — **confirmar** |
| Card-origin expense 2-tx | — | **Desvio aceito documentado pelo solicitante — confirmar no diff** |
| Key-less classified | — | Unpay/cancel sem chave classificados (unpay agora exige `paidTransactionId`) |

Cobertura verificada por execução local (2026-09-17): api 1941/1941 unit passed incluindo `tests/writes/domain-keyed-*`, `tests/routes/idempotency-route-identity.test.ts` e `tests/writes/postgres-idempotency-lease.test.ts` (race same-key ×6 → 1 efeito; failure-injection pós-efeito → retry converge; cross-id → 409).

## 3. Hash V2 + compat (Tasks 3.5–3.7)

- V2: `sha256("v2:" + canonicalJson)` — `hashPayloadV2` (`writes/idempotency.ts:96-99`); usado em `pending-idempotency.ts:73`, `in-memory.ts:226`, `postgres.ts:1170`.
- Legados legíveis: `hashIdempotencyPayload` (v1-sha256 top-level) e `legacyHashPayload` (h*31 32-bit) (`idempotency.ts:81-114`); comparação tolerante `matchesPayloadHash` (`:121-124`).

## 4. Command-id PWA + lost-response (Tasks 3.8–3.10)

- `apps/pwa/src/lib/api/command-id.ts:4,26-44` — chave por INTENÇÃO, não por tentativa; `retryMutationWithSameCommandId` (`:117-142`) preserva id em retry/timeout/reconnect.
- 409 = receipt original; erro carrega `commandId/lastCommandId` (`:64-95`); contrato em `command-id.test.ts:1-3,17-50`.
- Propagação: `client.ts:141-142,257-258`, `state/commands.ts:12-24,334-352`; regressão `endpoints.idempotency.test.ts:1-2`.

## 5. Review findings (relatados)

Cross-id replay corrigido, processing-lease, `failed-command-id` surfaced (`command-id.ts:70-83`) — **cruzar cada um com `git show --stat` do commit correspondente antes de fechar**.
