# V4.1 Closure Hardening — Relatório Final

**Data:** 2026-09-21
**SPEC:** `docs/MEU-TED-SPEC-V4.1-CLOSURE-HARDENING.md`
**PLANO:** `docs/MEU-TED-PLANO-V4.1-CLOSURE-HARDENING.md`
**PR:** https://github.com/Kusts/meu-ted/pull/11
**Branch:** `v4.1-hardening`

## SHA e escopo

| Item | Valor |
|---|---|
| Baseline declarada na SPEC | `main@7292dd896c2d34905e3747f4fc2fa53705b3bd48` |
| HEAD da branch no início da execução | `e0d5601` (= `7292dd8` + `1c097df` + `e0d5601`; nenhum item desta SPEC) |
| SHA final (código + review fixes) | `bf9005c849c65c5dfc903c181de0534ab3c459eb` |
| Commits da closure | 21 (`1bb241d..bf9005c`) |
| Arquivos alterados | 62 (+5.188 / −186) |

## Fase 0 — Baseline e público

- **Visibilidade pública verificada** (GitHub API não autenticada, `visibility=public`, 2026-09-21). Nenhuma alteração de visibilidade executada.
- **PUBLIC-HISTORY-01**: gitleaks 8.30.1 (`git --all`) — 738 commits varridos, **0 leaks** (re-executado após remover a exclusão ampla de `.github/workflows/` da allowlist, ver Fase 10/P2). Scan dirigido da árvore `.github`: 0 findings. Inventário de refs: `refs/pi-rewind/store` **não existe mais** (débito histórico resolvido); refs locais não publicadas restantes: `refs/stash` e `refs/codex/turn-diffs/*` (sem conteúdo sensível, 0 findings).
- **public-safety --strict**: PASS (0 findings, árvore publicável).
- **Baseline local**: typecheck PASS; units API 2.080 PASS, agent 541 PASS + 1 skip, broker 25 PASS, PWA 1.921/1.922 com 1 falha classificada como **flaky pré-existente** (`AuditPage > limpar filtros recarrega sem parâmetros` — passa isolado 3/3, fora do escopo; verde nas execuções finais completas).
- **Baseline PG**: banco de teste recriado do zero com bootstrap canônico (`_test_marker` = UUID do `DB_TEST_MARKER` + `db:migrate`; 55 migrations V001–V057 aplicadas). 2 falhas em `postgres-cards-v41-hardening` classificadas como **drift de fixture** (schema canônico construído à mão no teste não incluía `statement_payment_id` de V056/V057) — corrigidas em `3ba2bce`.

## Fases 1–3 — PWA: session-first + Offline Snapshot V3

Commits: `97f53ca`, `c674f3c`, `876ab20`, `8d81806`, `428a9d0`, `a7f1fcb` (+ fixes de review `08def61..c40d8c4`).

- **Autoridade de sessão explícita** (`lib/auth/session-authority.ts`): estados `authenticated`/`unauthenticated`/`unreachable`/`unknown`; `fetchSession()` preserva a distinção 401/403 vs rede (INV-05); `apiUsable()` destrava I/O online **somente** com sessão `authenticated` confirmada pela sonda — presença de bearer nunca é pré-requisito (INV-02), com `NEXT_PUBLIC_LEGACY_BEARER_COMPAT` funcional ON e OFF.
- **Identidade offline não-autenticadora** (`lib/auth/offline-identity.ts`): `offlinePrincipalId` + `offlineWorkspaceId` derivados da sessão autenticada (nunca de bearer/device/cookie); owner key `v3:principal:workspace`.
- **Envelope V3** (`lib/state/snapshot-db.ts`): slot `v3`, migração V2→V3 somente com sessão validada + owner provado + V2 legível/ dentro do TTL; caso contrário invalidação e re-sync (nunca atribuição heurística).
- **Roteamento**: `unreachable` + V3 válida dentro do TTL → offline read-only; `unreachable` sem V3 → login/offline sem purge; 401/403 → purge completo (incl. identidade V3) + login (INV-05/06). Offline writes continuam proibidos (nenhuma fila criada).
- **Purges**: logout/revogação/troca de workspace limpam V3, principal binding, workspace binding, subject, stamp e resíduos (AUTH-T07 — inclusive auto-switch por revogação, fix de review).
- **Testes**: AUTH-T01..T08 verdes (unidades) + XLT-02/08/09 verdes (32/32 no estado final).

## Fase 4 — API: atomicidade fail-closed

Commits: `3ba2bce`, `d13ef86`, `4139cb3`.

- Regra aplicada em **7 dispatchers + `applyReversal`** (24 ops keyed + 3 branches de undo): `claimTx presente + *InTx ausente → throw` `idempotency.atomic_mutation_not_supported` (HTTP 500, sem detalhes internos); fallback plain somente sem `claimTx` (INV-07).
- **`createLegacySubscriptionInTx`** implementado em `subscriptions/legacy-postgres.ts` (mesmo tx client do claim) — fecha o único gap real (DB_SCHEMA=legacy + Idempotency-Key).
- Testes que cristalizavam o fallback invertidos (`domain-keyed-dispatch`); provas PG estendidas (fail-closed com claim client real, twins canonical/legacy, rollback/commit).
- TX-T01..T06 verdes; concorrência 120/120; `integration:all` 153 PASS / 0 FAIL em DB fresco.

## Fases 5+7 — CI público + provenance de release

Commits: `c360883`, `eb275c6`, `3435b16`, `d679e34`, `bf9005c`.

- **Deploy gates** (`pwa-deploy.yml`, `agent-deploy.yml`): checagem explícita `head_repository == github.repository` (fail-closed) **e** `workflow_run.event == 'push'` (SPEC §14); todos os gates existentes preservados (conclusão, CI+PWA CI success no mesmo `head_sha`, stale-SHA, origens validadas).
- **`public-safety --strict`** promovido a required check (job próprio + `needs` no `gate`) — INV-08.
- **Branch protection em `main`** configurada via API (HTTP 200): required checks `Gate — all checks` + `quality (22, 10)`, `strict=true`, `enforce_admins=true`, sem force-push, sem deletion (SPEC CI-04/§15).
- **API provenance (REL-02)**: novo job `api-image` (somente push em `main`, `packages: write` job-scoped) publica `ghcr.io/kusts/meu-ted-api:sha-<sha>` e registra **digest** em output + artifact `api-image-provenance-<sha>.json`; runbook de deploy por digest adicionado a `docs/reports/v4.1-release-bridge-execution.md` §9. Sem automação de VPS, sem novos secrets (SPEC §16: STOP antes de deploy).
- **Agent manifest (REL-03)**: `{sha, workflow, runId, lockfileHash (sha256 pnpm-lock.yaml), wranglerVersion}`; verificação no deploy exige os campos novos (fail-closed).
- **Release manifest consolidado (REL-04)**: job `release-manifest` (main-push only) agrega os três apps em `release-manifest-<sha>.json` (versão simples, sem redesign de pipeline).

## Fase 6 — CI remoto novo (Gate P6)

Primeira rodada pós-publicação expôs **4 drifts de baseline** (o CI não rodava desde antes da publicação — exatamente o F4 da SPEC). Todos corrigidos com causa raiz, sem desabilitar checks (`d679e34`):

1. `DB_TEST_MARKER` do `ci.yml` não era UUID válido e o suite de goals o usa como household → marker alterado para `00000000-0000-4000-8000-000000000001`.
2. Smoke Docker: broker com boot fail-closed (V4.1 Phase 8) exigia `CODEX_SIGNING_KEY`/`BROKER_TRUST_PRIVATE_NETWORK` → literals descartáveis adicionados ao `scripts/container-smoke.mjs` (verificado localmente: smoke GREEN).
3. `sharp`: npm audit passou a emitir o registro com `effects: []` em algumas versões (shape alternativo) → entrada paralela na allowlist (`pwa-2026-09-21-sharp-audit-shape`, mesma exceção de risco, expira 2026-12-31).
4. `SMOKE-05` existia na spec `production-smoke` mas não na matrix de cobertura e2e → linha adicionada.

Resultado: **CI + PWA CI `success` no mesmo SHA** — verde em `d679e349` (PR #11) e novamente no SHA final `bf9005c` (runs: ver seção CI abaixo).

## Fase 8 — Reconciliação read-only (produção)

- Executada via túnel SSH documentado (`docs/ops/vps-access.md`) contra o banco de produção (`evolution-postgres/pi_financeiro`), com o CLI `pnpm reconciliation` (sessão com `default_transaction_read_only=on` + guard `isSelectOnly` em toda query — apenas SELECT; **nenhum INSERT/UPDATE/DELETE**).
- Resultado (2026-09-21T19:51Z, schema `legacy`): `accounts_balance` 20 checked / **1 drift**; `statement_total` 14/0; `statement_payment` 22/0; `payable_payment` 15/0; `goal_contribution` 12/0; `card_purchase` 47/0; `duplicates` 62/0.
- Classificação vs triagem histórica (`docs/reports/v4.1-reconciliation-triage.md`): o único finding é a conta `814332c4-…` (credit_card, saldo −R$ 560,00), **já documentada** como caso ambíguo (decisão do dono do histórico) → **`ambiguous` persistente; 0 `new-regression`**; os demais findings históricos não reapareceram.
- Gate P8: **PASS** (nenhuma regressão financeira nova inexplicada). Nenhum repair executado.

## Fase 9 — Documentação canônica

Commit `54894c3`.

- `docs/architecture/runtime-facts.json` regenerado pelo gerador oficial: `meu-ted-api`, 127 routes, **55 migrations, latest V057**, `lastVerified 2026-09-21`.
- `docs/ESTADO-E-PROXIMOS-PASSOS.md` e `docs/ops/g6-production-topology.md` marcados como **SNAPSHOT HISTÓRICO** (status/verifiedAt/supersededBy).
- `docs/ARCHITECTURE-CURRENT.md` atualizada (session-first, offline V3, keyed fail-closed, gates de deploy, GHCR por digest, manifest do Agent).
- Contratos: `pnpm docs:lint` PASS; `documentation-facts-contract` PASS; `canonical-docs-contract` PASS.

## Fase 10 — Auto-review independente

Duas revisões read-only paralelas (arquitetura/reliabilidade + segurança) sobre o diff completo. Veredito inicial: **CHANGES_REQUIRED** — 5× P1 + 1× P2, **0× P0**. Todos corrigidos e revalidados:

| # | Severidade | Achado | Correção |
|---|---|---|---|
| 1 | P1 | `apiUsable` ainda destrava I/O live por bearer em status `unknown`/`unreachable` | gate exige `authenticated` confirmado pela sonda (`52a593f`, `c40d8c4`) |
| 2 | P1 | device token + probe `unreachable` destravava sem validar V3/TTL | helper único `unlockOrLoginForUnreachable()` valida rota V3 (`27f868c`) |
| 3 | P1 | auto-switch por revogação não purgava `offlineWorkspaceId`/subject/stamp | `clearWorkspaceBinding: true` nos 2 caminhos + teste (`b417297`) |
| 4 | P1 | 401/403 sem token publicava login sem purge — snapshot V3 sobrevivia (INV-05) | `clearSensitiveSession` com `clearOfflineIdentity` antes do login + regressão exata (`27f868c`) |
| 5 | P1 | deploy gates não exigiam `workflow_run.event == 'push'` | condição adicionada nos 2 workflows (`bf9005c`) |
| 6 | P2 | allowlist do gitleaks excluía `.github/workflows/` inteiro | exclusão removida; re-scan de histórico (738 commits) e árvore `.github`: **0 leaks**, 0 falsos positivos (`bf9005c`) |

Re-validação pós-fixes: PWA focada 555 testes, full 1.966 testes, typecheck limpo, XLT 32/32.

## Validação consolidada (estado final)

- **Units**: API 2.012 PASS (+39 skips gated), PWA 1.966 PASS, agent 541 PASS + 1 skip, broker 25 PASS.
- **Postgres**: `integration:all` 153 PASS / 0 FAIL (DB fresco, V001–V057); concorrência 120 PASS.
- **XLT**: xlt-02 (3) + xlt-08 (6) + xlt-09 (2) — verdes.
- **Gates locais**: `typecheck`, `docs:lint`, `governance:check`, `public-safety --strict`, `action-pins:check` (79 pins), `test:skip-gate`, `root-ci-workflow` (16/16), `validate-deploy-workflows`, `security-gates`, contratos de docs — todos PASS.
- **CI remoto (PR #11)**: CI + PWA CI `success` no mesmo SHA (d679e349; re-green no bf9005c — IDs no PR).

## Decisões tomadas autonomamente (revisão retroativa bem-vinda)

1. Reutilização da branch `v4.1-hardening` (plano §0.2 permitia) — PR #11 contra `main`.
2. Bootstrap canônico do banco de teste documentado no fluxo (marker + migrate) após classificar as falhas iniciais como ambiente/fixture.
3. Marker do CI trocado para UUID válido (contrato do suite de goals) em vez de alterar o teste.
4. Dual-shape na allowlist do `sharp` em vez de flexibilizar o matcher fail-closed.
5. Literals descartáveis no smoke do broker com `BROKER_TRUST_PRIVATE_NETWORK=1` (container efêmero local-only; sem segredo real).
6. `idempotency.atomic_mutation_not_supported` mapeado para **HTTP 500** (invariante de servidor; revisão pode preferir 4xx — troca de 1 linha + asserções, ver débitos).
7. `release-manifest` consolidado implementado na versão simples permitida pela SPEC (REL-04).
8. Branch protection aplicada via API como parte do plano §5.5 (autorizada pelo plano; reversível).

## Débitos restantes e gates humanos

- **Merge do PR #11 em `main`** — autorização humana (branch protection já exige os checks).
- **Release B** — READY depende da janela temporal já definida: 14 dias consecutivos com **0 eventos** `auth.request.legacy_bearer_used` (consulta server-side; não simulável — §11.1 do plano).
- **Canonical Cutover** — fora desta SPEC (§21).
- **Finding `814332c4`** (credit_card −560) — decisão do dono (overdraft real vs receita faltante); nenhum repair executado.
- **HTTP 500 vs 4xx** para `atomic_mutation_not_supported` — decisão de contrato aberta na review (P3).
- Edge legado preservado de propósito: compat ON + verify-ok + probe `unauthenticated` destrava shell sem I/O (endurecer quando a janela legado fechar).
- `public/offline-shell.js` segue V2-only (fallback SW fora do bundle) — sem regressão; a reescrever quando o shell offline for tocado.
- Billing/minutes do GitHub Actions — monitorar (runs novos consomem quota).
- Deploys de produção (PWA/Agent/API) — **não executados**; exigem autorização humana separada.

## Rollback

Branch isolada + PR não mergeado: rollback = fechar o PR e apagar a branch remota. Nenhuma mudança de produção foi aplicada (API/PWA/Agent seguem nos SHAs de produção de 2026-09-18; nenhuma migration de banco foi criada nesta closure — V057 já existia na árvore). Branch protection pode ser revertida via Settings → Branches.
