# V4.1 Public Safety (Phase 6, Tasks 6.1–6.7, 2026-09-17)

Auditoria de segurança para publicação do repositório (SPEC Fase 6 / plano Phase 6 §10).
Branch `v4.1-hardening` @ `3b1a5a1` (17 commits à frente de `main`), working tree sujo
(trabalho V4.1 Phases 1–5 em andamento, não commitado). **Nenhuma mudança de
visibilidade foi executada ou autorizada; nenhum commit foi criado por esta tarefa.**

## 1. Método e escopo (Tasks 6.1–6.2)

- **gitleaks indisponível** neste host Windows (`Get-Command gitleaks` vazio) —
  mesma limitação registrada na readiness 2026-09-16 ("gitleaks skipped on win32,
  CI Linux é autoritativo"). Substituição por heurísticas equivalentes:
  - `git grep -E` na árvore HEAD por private keys, AWS/GitHub/OpenAI/Slack/Google/npm
    tokens (1 árvore completa, `HEAD -- .`);
  - `git log main..HEAD -S` (pickaxe, 17 commits V4.1) para o padrão de bloco de
    chave privada e para `VAPID_PRIVATE_KEY`;
  - `git log --all -- <path>` para o filename `.env.e2e.creds`;
  - `git ls-files` filtrado por `.env*`/`.dev.vars`/`.pem`/dumps/backups;
  - `git diff main HEAD --name-only` filtrado por `*env* *creds* *dump* *backup* *pem* *key*;
  - gate `pnpm public-safety` (default) + `node scripts/check-public-safety.mjs --strict`.
- **Escopo de refs publicáveis:** `git log --all` = 2678 commits; `main` = 689 commits;
  2 tags; branches locais `main` + `v4.1-hardening` (sem remotas além de `origin/main`).
  Sweep `-S/-G` completo do histórico não foi repetido neste host (timeout >120s no
  win32); a varredura completa de 18 padrões foi feita na readiness 2026-09-16 com
  veredito "nenhum segredo em refs alcançáveis" — esta sessão re-verificou a árvore
  atual, o delta V4.1 e a alcançabilidade dos objetos sensíveis conhecidos.

## 2. Segredos — resultado (Task 6.2)

**0 segredos reais em refs alcançáveis e na working tree trackeada.**

| Check | Resultado |
|---|---|
| `git grep` HEAD (keys/tokens) | 2 linhas, ambas fixture de redaction em `apps/agent/tests/transcript-safety.test.ts:12-13` (falso positivo conhecido, allowlisted no gate) |
| `git log main..HEAD -S '<padrão de bloco de chave privada>'` | vazio |
| `git log main..HEAD -S 'VAPID_PRIVATE_KEY'` | 1 hit: `27b3288` = adição da **regex de detecção** em `scripts/check-public-safety.mjs:34-37` (fonte do detector, não segredo; o próprio gate reporta 0 ERROR) |
| Filenames `.env.e2e.creds` em `--all` | nunca commitado em ref alcançável |
| `.env`/`.dev.vars`/`.pem`/dumps trackeados | nenhum (`git ls-files` vazio para esses padrões; só `.example`) |
| Filenames suspeitos no delta V4.1 | só `*keyed-mutations*` (idempotency-key pattern, benigno) |
| ERROR do gate `public-safety` | **0** |

## 3. Metadados — warnings, delta e triagem (Task 6.3)

`pnpm public-safety` (fiel, via pnpm): **PASS default, 0 ERROR, 83 WARN.**
`--strict`: **FAIL com os mesmos 83 WARN** (gate de publicação vermelho, como esperado).

Categorias atuais: `internal-hostname` 39, `production-ip` 16, `vps-host-or-ssh-user` 15,
`personal-email` 13. Nenhum WARN fora dessas 4 regras de metadados — **zero achado novo
com cara de segredo real** (item (c) da triagem = 0).

Reconciliação do delta (verificado por interseção arquivo-a-arquivo `main` × atual):

| Passo | Count | Explicação verificada |
|---|---|---|
| Readiness 2026-09-16 (era `main`) | 79 | baseline pré-V4.1 |
| Baseline `v4.1-baseline.md` (@`27b3288`) | 85 | +6 = auto-referências dos 2 novos reports (`v4.1-publication-readiness.md` +4 findings, `v4.1-runtime-inventory.md` +2) |
| Agora (@`3b1a5a1` + dirty) | **83** | −2 = `AGENTS.md` sanitizado na V4.1 (linha `Produção API (VPS deploy@187.77.249.47…)` da `main` removida; carregava 2 rule-findings: `production-ip` + `vps-host-or-ssh-user`) |

Contribuidores V4.1 confirmados (únicos arquivos de warning ausentes na `main`):
`docs/reports/v4.1-publication-readiness.md`, `docs/reports/v4.1-runtime-inventory.md`
e `scripts/check-public-safety.mjs` (linhas 52–53, fonte das regexes) — todos
**auto-referenciais**, esperados.

Triagem: (a) bloqueantes-para-publicação — todos os 83 (IP/SSH/e-mails pessoais/hostnames
internos, mesma natureza da readiness); (b) falsos positivos de segredo — os já
allowlisted com motivo no gate (fixtures de redaction, containers locais efêmeros);
(c) segredo real novo — **0**.

## 4. Histórico e objetos locais (Task 6.4 — SAFE ONLY)

- **Refs alcançáveis limpas:** nenhum segredo detectado; `.env.e2e.creds` jamais
  commitado em branch/tag.
- **Objeto local inalcançável `11b82e8`** ("pi rewind snapshot", contém `.env.e2e.creds`
  real de 5 linhas): **continua inalcançável** — `branch --contains` e `tag --contains`
  vazios; `merge-base --is-ancestor` falso para `HEAD` e para `main`. Risco de
  publicação via push: nenhum. Risco local: persiste (credenciais e2e de contas reais).
- **Stash `stash@{0}`** ("On main: pre-consolidation-merge"): contém só 5 arquivos
  (`.pi/AGENTS.md`, `.pi/tools/README.md`, `apps/pwa/package.json`, `package.json`,
  `pnpm-lock.yaml`) — **sem arquivo de credenciais dentro**. O objeto `11b82e8` pendura
  via snapshot/reflog local, não via este stash.
- **Remediação executada: nenhuma alteração de código** (correto: só warnings de
  metadados). **NÃO autorizado / NÃO executado:** `git stash drop`, `gc --prune`,
  rewrite de histórico, rotação de credenciais — todos exigem gate humano (§3.4 do plano).

## 5. Separação do CI público (Tasks 6.5–6.6)

- **`ci.yml` / `pwa-ci.yml`: zero referências a `secrets.*`** (grep verificado) —
  Postgres efêmero `test:test`, VAPID fake em fixtures. **SAFE_FOR_UNTRUSTED_PUBLIC_CI.**
- **`CLOUDFLARE_API_TOKEN` / `CLOUDFLARE_ACCOUNT_ID` isolados aos steps `Deploy …`
  dos jobs `deploy`** em `agent-deploy.yml:80-82` e `pwa-deploy.yml:88-92`.
  Jobs `gate` (só `actions: read`, fail-closed por SHA) e `smoke` (GETs públicos +
  Playwright read-only, sem secrets) verificados nos dois workflows.
- Deploys disparam só via `workflow_run` (`CI`+`PWA CI` success) em `branches: [main]` —
  fork-PRs não tocam em secrets.
- Débito aberto (inalterado, pré-publicação): jobs `deploy` sem `environment: production`
  com reviewers; única proteção atual = gate CI-por-SHA.

## 6. Postura do gate `public-safety` (Task 6.7)

Job `public-safety` presente no `ci.yml:312-324` e incluído no `gate:327-329`.
Postura confirmada: **default informativo no CI** (`pnpm public-safety`, PASS com
warnings listados); **bloqueante só com `--strict`** como gate de publicação
(plano §10.5–10.6). Estado atual: **PASS default / FAIL strict** — publicar exige
`--strict` verde + zero segredos + autorização humana.

## 7. Veredito e checklist restante (VISIBILIDADE NÃO AUTORIZADA)

**Phase 6: SCANS COMPLETOS — `secret findings = 0` em refs alcançáveis (exit gate §10
atendido na dimensão segredos); publicação continua NOT READY pelos metadados.**

Mudança de visibilidade **NÃO autorizada** nesta tarefa. Checklist pré-publicação
restante (da readiness, nada executado):
1. Sanitizar os 83 warns (parametrizar hosts/e-mails, defaults de admin por env
   obrigatória em prod, fixture CPF, origens Cloudflare para config) — sem rewrite;
2. `git stash drop` + `git gc --prune=now` (gate humano) + rotação opcional das senhas e2e;
3. `pnpm public-safety -- --strict` verde;
4. Endurecer deploys: `environment: production` com reviewers + `permissions: contents: read`;
5. Autorização humana → visibilidade → CI remoto verde em janela definida (re-privatizar se vermelho).
