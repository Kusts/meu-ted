# V4.1 Canonical Readiness — Phase 10 §14.5 (2026-09-17)

## Veredito: PRONTO PARA PLANEJAR O CUTOVER — NÃO PRONTO PARA EXECUTAR

Base: canônico semanticamente endurecido (fases 2–4), suite de paridade A/B/C verde ( Parts B/C PG-gated; rodar e anexar saída), shadow reads documentados sobre dados sintéticos, dry-run de migration **não executado**.

## Bloqueadores explícitos (todos exigem gate humano, plano §3.3–3.4)

1. **Release-bridge (§3.3):** produção V3/V052 → V4 (V053/V054 não aplicadas) → V4.1. Primeiro deploy carrega release composta; exige inventário, smoke/rollback por etapa e aprovação humana (`v4.1-release-bridge.md`).
2. **Migrations V053/V054 não aplicadas em produção** (top = V052; `v4.1-runtime-inventory.md:13`).
3. **Checksum re-backfill V022** pendente (follow-up; política WARN < V044).
4. **Shadow reads + reconciliação sobre dados REAIS** não executados (infra pronta: `reconciliation/` SELECT-only; requer túnel SSH + autorização).
5. **Migration dry-run** (§14.7) sobre cópia sanitizada não executado — pré-condição para `blockers = 0` (§20).
6. **Histórico ambíguo** (clamps podem ter materializado saldos divergentes do ledger) — reconciliação Fase 10 deve separar repair determinístico de histórico ambíguo (revisão humana).

## O que NÃO bloqueia

Paridade semântica, locks, idempotência single-tx, hash V2, gates de CI (VAL.14–18 no código), `public-safety` sem segredos. Cutover automático **proibido** (§3.4); trocar `DB_SCHEMA` sem os itens 1–5 é NOT READY por definição.
