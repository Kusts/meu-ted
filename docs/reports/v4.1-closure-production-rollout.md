# V4.1 Closure — Rollout de Produção (2026-09-22)

Execução autorizada pelo owner ("autorizo o restante") após o merge da closure (PR #11) e do fix de deploy (PR #12).

## 1. Tokens e deploys Cloudflare

- `CLOUDFLARE_API_TOKEN` anterior rejeitado pela API Cloudflare (`code 10000`) — deploys falharam fechados, produção intocada.
- Token rotacionado usando o cofre do gerenciador da conta (`D:\projetos\cloudflare\.env`, token full-access do owner; verificado via `/accounts` + listagem dos workers `pi-finance-pwa`/`pi-finance-agent`).
- **PWA Deploy**: success (deploy + smoke; `SMOKE-05` validou `/api/build-info.gitSha`).
- **Agent Deploy**: falhou 1ª rodada por `Could not resolve @pi-finance/llm-contracts` — o job de deploy nunca construía o pacote de contratos (buraco latente, primeiro deploy real pós-publicação). Corrigido no **PR #12** (`agent-deploy.yml` ganha o passo `Build shared contracts`, idêntico ao do CI); `pwa-ci.yml` ampliou paths de PR para `.github/workflows/**` (evita deadlock de required check em PRs de workflow).
- PWA e Agent deployados em `d5ba79d`.

## 2. Deploy da API na VPS por digest (runbook §9)

- Artefato de provenance do CI run `35748638534` (push `d5ba79d` em `main`):
  - image `ghcr.io/kusts/meu-ted-api`, tag `sha-d5ba79da…`, digest `sha256:bbc369303158bfd7357400a37a38da41d414acade847666667dce34e595b17ae`.
- Passos executados na VPS (usuário `deploy`, runbook `docs/ops/vps-access.md`):
  1. rollback tag: `pi-finance-api:rollback-pre-d5ba79d` (imagem anterior `e0d5601`);
  2. `docker pull` pelo digest;
  3. `docker tag … pi-finance-api:main`;
  4. `docker compose up -d pi-finance-api` (recriado);
  5. health: `starting → healthy` na 3ª checagem.
- **Verificação pública**: `https://api.synkroo.com.br/health` 200 = `{"gitSha":"d5ba79da…","buildId":"35748638534"}`; `/ready` 200.
- **Rollback**: re-apontar `pi-finance-api:rollback-pre-d5ba79d` + `docker compose up -d`.
- **Migrations**: produção segue em `_migrations` top **V054** (schema `legacy`). O binário `d5ba79d` é compatível com V054 por contrato (`e0d5601` — legacy startup contract). V055–V057 permanecem **pendentes**, a aplicar no escopo do Canonical Cutover (ou migrate-job dedicado autorizado), não neste rollout.

## 3. Release B (D11) — janela iniciada

- Telemetria `auth.request.legacy_bearer_used` (V054, sink `audit_logs`) ativa em produção desde **2026-09-18**.
- Consulta read-only executada em 2026-09-22: **0 eventos** (`SELECT COUNT(*) FROM audit_logs WHERE action = 'auth.request.legacy_bearer_used'` → 0).
- **Janela**: 14 dias contados de 2026-09-18 → gate em **2026-10-02**, exigindo 0 eventos no período. Repetir a consulta na data do gate; qualquer evento reinicia a contagem a partir dele.
- Fonte canônica: `docs/reports/v4.1-decision-gates.md` (D11).

## 4. Finding `814332c4` — dossier read-only

Ver `docs/reports/v4.1-finding-814332c4-dossier.md`. Resumo: conta "Cartão Itaú" (credit_card, saldo inicial 0) com exatamente **1 transação** (expense "carne", R$ 560,00, 2026-06-26) lançada direto na conta do cartão, fora do fluxo de statements/card_purchases. O saldo −56000 deriva integralmente desse lançamento. Decisão do dono pendente (duas opções documentadas no dossier).

## 5. Estado final de produção (2026-09-22)

| App | Ambiente | SHA | Verificação |
|---|---|---|---|
| API | VPS (docker, digest `bbc36930…`) | `d5ba79d` | `/health` 200 + `/ready` 200 |
| PWA | Cloudflare Workers | `d5ba79d` | smoke + `/api/build-info` |
| Agent | Cloudflare Workers (DO) | `d5ba79d` | `/health` 200 `ready`, `buildSha` |
| DB | evolution-postgres, schema `legacy` | migrations top V054 | reconciliação read-only 2026-09-21: 1 finding conhecido |

## 6. Gates restantes

1. **Release B** — observação até 2026-10-02 (automática, sem ação).
2. **Finding `814332c4`** — decisão do dono (dossier pronto).
3. **Canonical Cutover** — plano formal em `docs/superpowers/plans/2026-09-22-canonical-cutover.md` (aguarda aprovação; não executado).
4. Débitos herdados: token CI full-access → trocar por escopo de deploy; billing GitHub Actions; itens future-dated.
