# V4.1 CI Validation — Phase 9 (2026-09-17)

Phase 9 do plano (§13, Tasks 9.1–9.11). Evidências verificadas no código; execuções quantitativas abaixo **não rodadas neste ambiente** (sem shell) — comandos indicados.

## 1. Novos scripts/gates (verificado)

| Item | Evidência |
|---|---|
| `test:integration:all` (toda `tests/integration/**`) | `apps/api/package.json:15` |
| `test:concurrency` (config dedicada) | `apps/api/package.json:16` |
| `test:xlt` | `apps/api/package.json:17` |
| `check-test-skips` + allowlist | `scripts/check-test-skips.mjs:14,145` |
| VAL.14–18 em `run-final-validation.mjs` | `:28-32` (integration:all, concurrency, XLT, skip+pin gates, migration-validate + `security:deps`) |
| `migrate --validate` | `scripts/migration-validate.mjs` (VAL.18) |
| `check-action-pins` + dependabot | `scripts/check-action-pins.mjs:15` (+ Task 9.11; `GITHUB_TOKEN` mínimo — **confirmar workflows**) |
| Release identity API (`gitSha/buildId/builtAt`) | `apps/api/src/routes/index.ts:479-487` |

## 2. Artefatos e gates de deploy (verificação pendente de diff)

Estratégia relatada: PWA build-once artifact + agent manifest source-verified; same-SHA stale check (`candidate == main`); concurrency unificada; smoke de SHA via identity endpoints. **Confirmar nos arquivos de workflow** com: `git diff dd10e2b..HEAD --stat -- .github/workflows` + leitura dos jobs `deploy`/`gate`/`smoke`.

## 3. V022 + drift policy

V022 in-place fix + política WARN < V044 e re-backfill follow-up — **confirmar via** `git log -S 'V022' --oneline dd10e2b..HEAD` e leitura da migration. Drift de checksum pré-existente documentado (`v4.1-baseline.md:18`).

## 4. Prova `integration:all` green-on-fresh (relatada, não verificada aqui)

29 arquivos / 142 testes / 0 falhas × 2 runs. Reproduzir com: `pnpm --filter meu-ted-api test:integration:all` (requer `DATABASE_URL_TEST` + `DB_TEST_MARKER`, banco recriado) e colar saída. Totais unit (api/pwa/agent/broker/llm-contracts/concorrência/XLT) idem — **não repetir números sem saída de comando**.

## 5. CI remoto

**Bloqueador externo permanece:** billing/spending limit do GitHub Actions (startup failure; cf. `v4.1-runtime-inventory.md:31`, `v4.1-public-safety.md` §5–6). Deploys seguem manuais (wrangler/VPS). `public-safety` default PASS / `--strict` FAIL com 83 WARN (metadados, `v4.1-public-safety.md:44`).
