# P3 — Gate T+36h/T+48h e Checkpoint (2026-08-25)

**Data:** 2026-08-25T12:55Z
**Branch:** `fase-0-preparo` (121 commits à frente de `main`, `764d2e5`)
**Gate:** `scripts/g6-soak-status.mjs --gate T+36h/T+48h` + `docs/ops/g6-48h-gate.md` + `docs/ops/g6-legacy-retirement-change-set.md`

## 1) Inventário legado (sem remoção)

```bash
node scripts/plan-legacy-retirement.mjs --inventory
# Generated change-set document at docs/ops/g6-legacy-retirement-change-set.md

node scripts/check-legacy-runtime-references.mjs --stage=final
# === Legacy Runtime References Scan (final) ===
# Total References Found: 4
# Active Runtime:         0
# Rollback-Only:          4
# Secret Names:           0
# Gate Evaluation:        PASSED ✅
```

| Alvo | Tamanho | Estado |
|---|---|---|
| `apps/whatsapp-bridge/` | 123 arquivos | `rollback-only` — parado `pi-stack Exited`, não removido |
| `.pi/extensions/financial-tools/` | 3065 arquivos | `rollback-only` — arquivado em tag, não removido |
| `pnpm-workspace.yaml` / `package.json` scripts | — | `rollback-only` — refs preservadas |
| `EVOLUTION_API_KEY` | — | `rollback-only` — não rotacionado |

> Fail-closed: `git rm -r apps/whatsapp-bridge` e `check-legacy-runtime-references --stage=final` só após gate + autorização (`docs/superpowers/specs/2026-08-16-project-pending-closure-design.md:184`).

## 2) Checkpoint de rollback (antes de qualquer deleção)

```bash
git tag rollback-pre-p3-2026-08-24 a95a153
git push origin rollback-pre-p3-2026-08-24
# a95a153fc60fa3116a6749350aee612e56a16cdc  refs/tags/rollback-pre-p3-2026-08-24

git tag pre-g6-legacy-retirement 332b4a0
git push origin tag pre-g6-legacy-retirement
# * [new tag] pre-g6-legacy-retirement -> pre-g6-legacy-retirement
# Pre-cutover snapshot: antes da desativação do bridge (pi-stack stopped 2026-08-19)
```

Restauração:
```bash
git checkout pre-g6-legacy-retirement -- apps/whatsapp-bridge
git checkout rollback-pre-p3-2026-08-24 -- .pi/extensions/financial-tools
docker compose up -d whatsapp-bridge
```

## 3) Gate T+36h / T+48h

```bash
node scripts/g6-soak-status.mjs --gate T+36h
# === G6 48-Hour Independence Gate Status ===
# Status:          IN_PROGRESS
# Elapsed Hours:   10.91h
# Remaining Hours: 37.09h
# Can Close:       NO (In Progress / Vetoed) ⏳
#  - Window in progress: 10.9h / 48.0h elapsed (37.1h remaining)

node scripts/g6-soak-status.mjs --gate T+48h
# Status: IN_PROGRESS 10.91h/48h — mesmo
```

`docs/ops/g6-48h-gate.md` (gate 2026-08-18):

| Checkpoint | Status |
|---|---|
| T+0h 2026-08-18T17:15:55Z | PASS |
| T+1h | PASS |
| T+6h | PASS |
| T+12h | PASS |
| T+24h | PASS |
| T+36h 2026-08-20T05:15:55Z | PASS |
| T+48h | PENDING |

Independência verificada: `https://api.synkroo.com.br/health` 200 OK, `https://<PWA_HOST>/pwa-control` 200 OK, `check-legacy-runtime-references` 0 active — zero tráfego bridge.

Stage 2 do change-set (`Verify 100% PWA/Agent Worker, zero bridge traffic`) validado via `npx tsx scripts/cutover-check.ts` READY FOR CUTOVER 5/5 e gate checkpoints.

## 4) Próximos passos destrutivos (bloqueados até gate + consent)

| Stage | Ação | Prova | Estado |
|---|---|---|---|
| 1 | Disable Evolution Webhook | `curl POST /webhooks/evolution 404` | ✅ EXECUTADO 2026-08-19 `pi-stack Exited` |
| 2 | Observe Agent Owner 48h | `cutover-check.ts` + gate 48h | ⏳ IN_PROGRESS 10.9h/48h |
| 3 | Stop/Remove Bridge Service | `docker ps \| grep -v bridge` | ⏳ Parado, não removido (`rm -f` aguarda gate) |
| 4 | Remove Bridge Source | `git rm -r apps/whatsapp-bridge` + `test ! -d` | 🔒 BLOQUEADO |
| 5 | Archive Financial Tools | `git rm .pi/extensions` | 🔒 BLOQUEADO |
| 6 | Remove Residual Configs | `pnpm install && pnpm build` | 🔒 BLOQUEADO |
| 7 | Rotate Secrets | `pnpm security:secrets` | 🔒 BLOQUEADO |

Nenhuma deleção irreversível executada nesta sessão — conforme `AGENTS.md:70` e `plan:184`.
