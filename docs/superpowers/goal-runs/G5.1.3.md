# Goal run — G5.1.3

**Status:** done

## Contract

Revalidate session/membership on every workspace message.

## Evidence

`apps/agent/src/index.ts` applies the membership pre-handler to both the workspace route and `/message` route on every Worker fetch; no membership result is cached in the Worker or Durable Object. A revoked member therefore fails before the DO lookup on the next message.

## Verification

- `pnpm --filter pi-finance-agent test`: 7/7 passed.
- `pnpm --filter pi-finance-agent typecheck`: passed.
- `agent-auth.test.ts` proves first message resolves the DO, second message revalidates API membership, returns 403 after revocation, and does not resolve a second DO.
