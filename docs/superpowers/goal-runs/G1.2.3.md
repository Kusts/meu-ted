# Goal run — G1.2.3

**Goal ID:** `G1.2.3`
**Status:** `done`

## Coverage contract

PWA coverage enforces per-file thresholds of 80% for statements, branches, functions, and lines across the complete changed-production inventory in `apps/pwa/vitest.config.ts`. `all: true` keeps untested changed files visible; no test skip was added.

## Changes

- Added coverage for persisted transaction drafts, corrupt drafts, profile default hydration, profile save failure/discard flows, chat sheet, SW runtime branches, SW coordinator lifecycle/update paths, API income/transfer paths, non-fatal profile/insight reads, missing-record guards, and offline account/category mutations.
- Bundle measurement now identifies current hashed framework root chunks from `build-manifest.json#rootMainFiles`; historical prefix fixtures remain covered, while the live OpenNext output no longer depends on stale hard-coded chunk IDs.

## Verification

- `pnpm exec vitest run --coverage` in `apps/pwa`: **66 files, 1,040 tests passed**.
- Coverage: **91.04% statements, 85.25% branches, 90.71% functions, 94.26% lines**.
- Bundle budget test: passed with manifest-derived framework classification.
- `git diff --check`: passed.
