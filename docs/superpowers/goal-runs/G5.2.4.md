# Goal run — G5.2.4

**Goal ID:** G5.2.4
**Started:** 2026-08-04T13:57:00Z
**Completed:** 2026-08-04T14:35:00Z
**Status:** done

## Contract

- **Objective:** Rodar shadow read-only contra respostas Pi e registrar divergências.
- **Acceptance:** somente capabilities de leitura disparam o shadow; a resposta canônica/API do Pi não é substituída; legacy usa apenas `SELECT`; divergências/falhas são registradas com hashes bounded, deduplicação e redaction, sem dados brutos.

## Evidence log

| Continuation | Command/result | Failure signature | Decision |
|---:|---|---|---|
| 0 | Inspeção encontrou shadow runner, logger, seis readers legacy e integração gerada nas GETs | comparação incluía envelope HTTP (`success/items/total`) | corrigir canonicalização |
| 1 | Teste RED reproduziu divergência falsa para resposta real projetada | wrapper API e payload legacy tinham formas diferentes | remover somente campos de transporte da comparação |
| 2 | Readers alinhados aos campos `x-pi-tool.result`; divergência real continua gerando hashes | — | aceitar |
| 3 | Auditoria identificou falsos positivos em `get_balance` e filtros sem equivalente | metadata e filtros eram comparados/ignorados incorretamente | normalizar semântica e declarar filtros excluídos |
| 4 | Teste end-to-end do tool gerado passou com shadow habilitado, divergência registrada e resposta preservada | — | aceitar |
| 5 | Auditoria apontou semântica real de audit legacy e `audit_logs.limit` não permitido | evento/ator legacy e allowlist incompletos | normalizar `legacy.<action>`, `legacy-unknown` e permitir limit |

## Final verification

- [x] Integração gerada chama `runShadowRead` somente quando `spec.shadow` é true; writes não importam shadow.
- [x] `shadow-read.ts` normaliza metadata de `get_balance`/audit logs, compara resposta API/Agent com projeção legacy e registra apenas `requestHash`, `apiHash`, `legacyHash`.
- [x] `legacy-readers.ts` executa apenas SQL `SELECT`; testes verificam ausência de writes.
- [x] `shadow-logger.ts` grava JSONL bounded, deduplica eventos e redige credenciais.
- [x] `PI_SHADOW_READS` controla modo; inválido falha aberto sem alterar resposta; filtros sem equivalente são explicitamente ignorados por `shadow-filters.ts`.
- [x] `pnpm --dir .pi/extensions/financial-tools test` — 36/36.
- [x] `pnpm --dir .pi/extensions/financial-tools typecheck` — passou.

## Blockers / follow-up

- [x] Audit logs equal semântico (`legacy.<action>`, `legacy-unknown`) não geram divergência.
- [x] `audit_logs.limit` é aceito pelo filtro shadow e invoca o reader.

- G5.2.5 deve liberar writes por capability com intention ID persistente.
