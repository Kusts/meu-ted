# Goal run — G5.1.6

**Status:** done

`WorkspaceAgent` persists turns in `turn_queue`, token budget/usage in `token_usage`, claims work with leases and attempt increments, consumes queued work from the Durable Object alarm, executes a bounded processor, retries failures up to three attempts, propagates abort signals, exposes actor-scoped cancel/retry, and persists `turn_events` for cursor-based SSE replay. Worker routes message/stream/abort/process/retry URLs through the same auth pre-handler. PWA `agent-client.ts` supports send, process, SSE reconnect with cursor and dropped-connection retry, cancel and retry; the transcript UI exposes the complete send-to-assistant-output lifecycle.

Retry preserves the global attempt count, resets only output token usage to the persisted input baseline, rejects exhausted retries, reconnects to terminal SSE events, and appends the retried assistant output. Empty SSE snapshots are polled with the cursor until terminal state or bounded polling expiry; dropped connections are retried. The alarm drains all queued turns and reschedules only while work remains. If `/process` loses connectivity, the PWA reconnects with the queued turn ID and recovers the terminal assistant event/output. Completed turns return persisted `output_json` when the alarm wins the `/process` race; process output is rendered before best-effort follow-up reconnect.

Verification: `pnpm --filter pi-finance-agent test` 19/19; agent and PWA typechecks pass; PWA agent-client tests 3/3 and transcript tests 4/4. `agent-queue.test.ts` covers multi-turn alarm draining, alarm-before-process persisted output, persisted token budget/output enforcement, retry budget, persisted SSE cursor events, process completion, abort propagation, stale recovery and exhausted budget; transcript tests cover process-network-loss recovery and retry output.
