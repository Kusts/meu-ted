# Goal run — G5.1.4

**Status:** done

`getAgentByName()` maps the workspace UUID/name directly to the Durable Object namespace ID. Personal and shared workspaces use the same stable one-DO-per-workspace identity; collaboration mode is a domain behavior, not a second object identity. `WorkspaceAgent` now persists user messages in SQLite and returns shared history to authorized actors; separate DO instances remain isolated for personal workspaces.

Verification: `pnpm --filter pi-finance-agent test` 10/10 and `pnpm --filter pi-finance-agent typecheck` passed. `workspace-routing.test.ts` proves stable identity; `agent-chat.test.ts` proves actor-required writes, shared multi-actor history, and separate personal workspace isolation.
