# Goal run — G1.1.5

**Goal ID:** `G1.1.5`
**Status:** `done`

## Contract

- **Objective:** corrigir os 7 grupos de regressão API e 2 grupos PWA sem usar skips.
- **Acceptance:** `pnpm test` verde, zero testes skipped no comando padrão, causa raiz registrada.

## Root-cause map

| Target | Root cause | Permanent fix |
|---|---|---|
| API `auth/device-token.test.ts` | testes ainda assumiam bootstrap anônimo e revogação sem identidade autenticada | contrato atualizado para registro desabilitado e self-revocation com `X-Device-Token` |
| API `contract/idor-cross-household.test.ts` | fixtures/requests não exercitavam a fronteira de household | fixtures e assertions passaram a usar tokens/households distintos |
| API `routes/accounts-write.test.ts` | setup de escrita não carregava contexto de token/household | setup usa app autenticado e escopo correto |
| API `routes/cards.test.ts` | fixtures misturavam contas bancárias/cartão e referências de compra | fixtures e assertions respeitam `kind=credit_card` e account scope |
| API `routes/categories-write.test.ts` | payload/expectativas não refletiam categoria tipada por `kind` | payloads e assertions alinhados ao contrato atual |
| API `routes/payables.test.ts` | datas fixas ficavam inválidas conforme o relógio corrente | helpers `thisMonth`/`nextMonth` tornam o caso determinístico |
| API `routes/transactions-write.test.ts` | setup usava IDs/escopo e payloads antigos | setup e payloads alinhados ao write-store atual |
| PWA `src/__tests__/proxy-utils.test.ts` | middleware Next 16 exige `NextRequest`, não `Request` genérico | fixture usa `new NextRequest()` |
| PWA `src/headers.test.ts` | contrato dependia exclusivamente de artefato OpenNext já gerado | valida `.open-next/assets/_headers` ou `public/_headers` canônico; não há skip |

## No-skip policy

- O comando padrão `apps/api test` exclui somente os testes de integração Postgres por serem dependentes de banco externo; eles permanecem executáveis via `pnpm --filter pi-finance-api test:integration`.
- Os testes de integração não usam `describe.skip`, `it.skip` ou `skipIf`; sem `DATABASE_URL_TEST`, a execução explícita falha de forma visível.
- Production smoke opt-in registra um teste de guarda quando desabilitado, em vez de skip.

## Final verification

- `pnpm test`: API 31 arquivos/297 testes, bridge 41/447, PWA 65/1017; zero skipped.
- `pnpm lint`: passou.
- `pnpm typecheck`: passou.
- `pnpm build`: passou.
- Busca de `describe.skip`, `it.skip`, `test.skip`, `skipIf` nos testes API/PWA: nenhum resultado.
