# Goal run — `G0-T1`

**Goal ID:** `G0-T1`
**Started:** `2026-07-29T20:00:00Z`
**Completed:** `2026-07-29T20:45:00Z`
**Status:** `done`

## Contract

- **Objective:** provar o cap do loop e a parada por estagnação.
- **Allowed files:** `scripts/goal-loop-red-baseline.mjs`, `scripts/goal-loop-red-baseline.test.mjs`, `scripts/verify-goal-loop-policy.mjs`, `scripts/verify-goal-loop-policy.test.mjs`, `package.json`, este arquivo.
- **RED command and expected failure:** `node --test scripts/goal-loop-red-baseline.test.mjs` contra a baseline histórica sem cap/estagnação.
- **Acceptance:** cap em 6 continuações; falha idêntica repetida para em 2.
- **Unit/contract tests:** `node --test scripts/verify-goal-loop-policy.test.mjs`.
- **Typecheck/lint:** não aplicável ao script standalone; `git diff --check` obrigatório.
- **Limits:** máximo 6 continuações, 45 minutos, parar após a mesma falha duas vezes.

## Evidence log

| Continuation | Command/result | Failure signature | Decision |
|---:|---|---|---|
| 0 | RED: `node --test scripts/goal-loop-red-baseline.test.mjs` | 2 failures: baseline retornou `unfinished` em vez de `continuation-cap` e `repeated-failure` | implementar política bounded |
| 1 | GREEN: `node --test scripts/verify-goal-loop-policy.test.mjs` | `2/2 passed` | verificar ambos os limites |
| 2 | GREEN: `pnpm goal-loop:check` | `cap=6, repeated-failure=2` | encerrar dentro do limite |

RED output completo: `docs/superpowers/goal-runs/G0-T1-red.txt`.

Resumo do RED capturado:

```text
not ok 1 - RED: baseline must stop at the required continuation cap of 6
actual 'unfinished' / expected 'continuation-cap'
not ok 2 - RED: baseline must stop after repeated identical failure
actual 'unfinished' / expected 'repeated-failure'
# pass 0
# fail 2
```

## Final verification

- [x] Acceptance: 7 steps retornam `continuation-cap`, `continuations=6`.
- [x] Stagnation: duas falhas `typecheck` retornam `repeated-failure`, `continuations=2`.
- [x] `node --test scripts/verify-goal-loop-policy.test.mjs` — 2/2 passou.
- [x] `pnpm goal-loop:check` — passou.
- [x] RED fixture e saída completa permanecem versionados.
- [x] Diff revisado; nenhum guard/assertion removido.
- [x] Snapshot não aplicável.

## Blockers / follow-up

- Nenhum blocker. A baseline RED é somente fixture histórica; a política executada é `verify-goal-loop-policy.mjs`.
