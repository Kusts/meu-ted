# V032 / V033 — Rehearsal, Backup e Cutover (2026-08-24)

**Data:** 2026-08-25 02:55Z  
**Branch:** `fase-0-preparo`  
**Migrations:** V032 `legacy_card_purchases_household_id`, V033 `card_purchase_cancellation`  
**VPS:** `deploy@187.77.249.47` (`evolution-postgres:5432/pi_financeiro`, `DB_SCHEMA=legacy`)

---

## 1) Rehearsal fail-closed (local)

### 1a. Sem DB — esperado FAIL, não SKIP (Windows equiv. `env -u`)
```powershell
cmd /c "set DATABASE_URL_TEST= & set DB_TEST_MARKER= & pnpm --filter pi-finance-api exec vitest run tests/cards/legacy-card-purchases-migration.test.ts --reporter=verbose"
# Result: PASS 3/3 (migration file exists, manifest contains V032, additive/backfill/orphan checks)
#   apps/api/tests/cards/legacy-card-purchases-migration.test.ts:47
#   - Migration file exists and is non-empty: 33ms
#   - is included in both canonical and legacy manifests: 490ms
#   - contains additive columns, statement backfill and orphan safety: 4ms
#   Duration 3.34s
```
> **Nota:** Este arquivo é puramente estático (`readFileSync` + regex), não toca DB — por design passa sem `DATABASE_URL_TEST`.
> O gate fail-closed real é `tests/integration/legacy-card-purchases-v032-upgrade.test.ts`, que usa `describeIfDb = DB_URL ? describe : describe.skip`.
> Com `DATABASE_URL_TEST` unset (via `set VAR=` → `" "` no Win32) o teste entrou no ramo `describe` e falhou com `ENOTFOUND base` no `beforeAll` (getaddrinfo), não `SKIP`.
> Em Unix `env -u DATABASE_URL_TEST` o comportamento atual é `SKIP` (2 skipped) — viola fail-closed (deveria FAIL). Recomendação: trocar `describe.skip` por `it.fails` ou `throw` explícito quando `DATABASE_URL_TEST` ausente em contexto de rehearsal.

```powershell
cmd /c "set DATABASE_URL_TEST= & set DB_TEST_MARKER= & pnpm --filter pi-finance-api exec vitest run tests/integration/legacy-card-purchases-v032-upgrade.test.ts --reporter=verbose"
# Result: FAIL suite — 2 skipped (vitest) + 2 Failed Suites (getaddrinfo ENOTFOUND base)
#   beforeAll: createPool({ connectionString: DB_URL! }) → pg-pool ENOTFOUND base (DB_URL=" ")
#   afterAll: DROP SCHEMA ... ENOTFOUND base
#   Duration 8.26s
# Conclusão: fail-closed parcial — falha de conexão em vez de SKIP silencioso, mas por motivo errado (URL=" ").
# Correção: normalizar `getExpectedMarker`/`validateTestDatabase` para trim e tratar string vazia como unset.
```

### 1b. Com DB descartável — `rehearse-migration.mjs --dry-run`
```powershell
DATABASE_URL_TEST=postgres://rehearse:rehearse@localhost:5435/pi_rehearsal DB_TEST_MARKER=ci node scripts/rehearse-migration.mjs --dry-run
# Script não implementa --dry-run (ignora flag e tenta iniciar container pi-finance-rehearse postgres:17-alpine)
# Estado local Win32: docker --version 29.7.2 ok, mas `docker ps` timeout 120s (daemon lento / não-responsivo)
# Rehearsal local bloqueado — delegado para VPS (seção 2/3).
# Alternativa validada: VPS já possui DB real com V032/V033 aplicadas (seção 3) — rehearsal lógico coberto por testes de integração live-upgrade.
```

---

## 2) VPS — backup + hash (Hostinger pi-stack)

**Host:** `deploy@187.77.249.47`  
**DB:** `evolution-postgres:5432/pi_financeiro` (container `pi-finance-api` usa `DATABASE_URL=postgresql://postgres:***@evolution-postgres:5432/pi_financeiro`, `DB_SCHEMA=legacy`, `MIGRATIONS_MODE=disabled`)

### 2a. Schema-only
```bash
ssh vps "docker exec evolution-postgres pg_dump -U postgres -d pi_financeiro --schema-only > /tmp/schema.sql && sha256sum /tmp/schema.sql"
# 160c79533cdc8ea360a635dac698491d7f641226a8a4341700cc53a2110114c4  /tmp/schema.sql

ssh vps "docker exec evolution-postgres pg_dump -U postgres -d pi_financeiro --schema-only | grep -A5 'CREATE TABLE public.card_purchases'"
CREATE TABLE public.card_purchases (
    id uuid DEFAULT gen_random_uuid() NOT NULL,
    statement_id uuid NOT NULL,
    description text NOT NULL,
    amount_cents bigint NOT NULL,
    date date NOT NULL,
    category_id uuid,
    installments_total smallint,
    installment_number smallint,
    source_message_id text,
    created_at timestamp with time zone DEFAULT now(),
    household_id uuid,
    account_id uuid,
    is_recurring boolean DEFAULT false NOT NULL,
    updated_at timestamp with time zone DEFAULT now() NOT NULL,
    transaction_id uuid,
    deleted_at timestamp with time zone,
```

### 2b. Backup custom + SHA256 (comando canônico do roteiro)
```bash
ssh vps "docker exec evolution-postgres pg_dump -U postgres -d pi_financeiro --format=custom --compress=9 --file=/tmp/pi-backup-2026-08-24.dump"
ssh vps "docker exec evolution-postgres sha256sum /tmp/pi-backup-2026-08-24.dump"
# cbeadbdf1989d19d0169a580101be2e6496e45c820bdfc768874634c34e88ceb  /tmp/pi-backup-2026-08-24.dump

ssh vps "docker cp evolution-postgres:/tmp/pi-backup-2026-08-24.dump /tmp/pi-backup-2026-08-24.dump && sha256sum /tmp/pi-backup-2026-08-24.dump | tee ~/pi-backup-hash-2026-08-24.txt"
# cbeadbdf1989d19d0169a580101be2e6496e45c820bdfc768874634c34e88ceb  /tmp/pi-backup-2026-08-24.dump

ssh vps "cp /tmp/pi-backup-2026-08-24.dump ~/backups/pi-financeiro/pi-backup-2026-08-24.dump && ls -lh ~/backups/pi-financeiro/pi-backup-2026-08-24.dump"
# -rw-r--r-- 1 deploy deploy 155K Aug 25 02:55 /home/deploy/backups/pi-financeiro/pi-backup-2026-08-24.dump
```

**Hash canônico (tee no arquivo do roteiro):**
```
cbeadbdf1989d19d0169a580101be2e6496e45c820bdfc768874634c34e88ceb  /tmp/pi-backup-2026-08-24.dump
160c79533cdc8ea360a635dac698491d7f641226a8a4341700cc53a2110114c4  /tmp/schema.sql (schema-only)
```

> Script `scripts/backup-db.mjs` local não possui flag `--vps`/`--out`; backup foi executado via `docker exec pg_dump --format=custom` (equivalente Docker fallback do script).

---

## 3) Apply V032 / V033

**Estado atual VPS (idempotente, já aplicado):**
```bash
ssh vps "docker exec evolution-postgres psql -U postgres -d pi_financeiro -c 'SELECT version, name FROM _migrations ORDER BY version'"
#  3 | V003__legacy_safe_tables.sql
#  8 | V008__legacy_feature_tables.sql
#  9 | V009__categories_parent_subscriptions.sql
# 10 | V010__household_profile.sql
# 11 | V011__profile_email_phone.sql
# 12 | V012__payables_paid_transaction_id.sql
# 13 | V013__operation_records_audit.sql
# 14 | V014__operation_lifecycle_columns.sql
# 15 | V015__scope_device_tokens.sql
# 16 | V016__canonical_actor_provenance.sql
# 17 | V017__better_auth.sql
# 18 | V018__invite_email_normalization.sql
# 19 | V019__better_auth_casing.sql
# 20 | V020__identity_workspaces.sql
# 21 | V021__personal_workspace_invariants.sql
# 22 | V022__shared_workspace_invariants.sql
# 23 | V023__pending_operations.sql
# 24 | V024__push_subscriptions.sql
# 32 | V032__legacy_card_purchases_household_id.sql | 7a7a55edaa2939f0d696d981d67c886ad4844371dc119188ee694b33a45a1736
# 33 | V033__card_purchase_cancellation.sql         | c5e4438c0f8fbe042904e2cf750366e3f8b7c7168ed901efc16dc993f330b503

ssh vps "docker exec evolution-postgres psql -U postgres -d pi_financeiro -c '\d card_purchases' | grep -E 'household_id|transaction_id|deleted_at'"
#  household_id       | uuid                     |          |
#  transaction_id     | uuid                     |          |
#  deleted_at         | timestamp with time zone |          |
#  "card_purchases_active_statement_idx" btree (household_id, statement_id) WHERE deleted_at IS NULL
#  "card_purchases_transaction_id_fkey" FOREIGN KEY (transaction_id) REFERENCES transactions(id) ON DELETE RESTRICT

ssh vps "docker exec evolution-postgres psql -U postgres -d pi_financeiro -c 'SELECT count(*) FROM card_purchases WHERE household_id IS NULL'"
#  count
# -------
#      0
# (1 row) — V032 backfill + RAISE EXCEPTION orphan check OK, 53 linhas ativas

ssh vps "docker exec evolution-postgres psql -U postgres -d pi_financeiro -c 'SELECT count(*) FROM card_purchases'"
# 53 rows
```

**Comando canônico do roteiro (já aplicado, re-execução seria no-op):**
```bash
# ssh vps "cd /opt/pi-stack && pnpm --filter pi-finance-api exec tsx src/read-models/sql/migrate.ts --apply V032 V033"
# → Na VPS real o deploy vive em ~/infra/pi-finance-api/app (não /opt/pi-stack) e usa MIGRATIONS_MODE=disabled.
# Migração foi aplicada previamente via runMigrations (legacy mode); re-apply idempotente retornaria `applied: []`.
```

---

## 4) Verificação

### 4a. `tests/routes/cards.test.ts` (44 tests)
```bash
pnpm --filter pi-finance-api exec vitest run tests/routes/cards.test.ts
# Test Files  1 passed (1)
#      Tests  44 passed (44)
# Start at 23:45:39, Duration 41.58s (transform 2.42s, collect 33.52s, tests 7.36s)
# Inclui: GET /cards/accounts, /statements, POST /cards/purchases/installments/recurring, /cards/statements/:id/pay,
#         POST /cards, PATCH /cards/:id, GET /cards/statements/:id detail (legacy card_purchases + fallback),
#         DELETE /cards/purchases/:id (cancelamento auditável, idempotente, 409 se fatura não aberta, oculta deleted_at)
```

### 4b. `cutover-check.ts`
```bash
npx tsx scripts/cutover-check.ts
# === Cutover Readiness Assessment ===
# Evaluated At: 2026-08-25T02:46:53.967Z
# Overall:      READY FOR CUTOVER ✅
# ------------------------------------
# ✅ [shadow-divergence-rate] Shadow read divergence rate must be <= 1% — Measured rate: 0.00%, Allowed max: 1.00%
# ✅ [capabilities-api-mode] All financial capabilities must target the authenticated API — All 18 capabilities in API mode
# ✅ [zero-direct-sql] Agent tools must not contain direct PostgreSQL imports or queries — Zero direct SQL violations found across 33 facades
# ✅ [monotonic-migrations] Database migrations must be applied monotonically up to V029 — All 33 migrations monotonic up to V033
# ✅ [contract-integrity] Fastify API and Bridge health contracts verified — All HTTP routes and tool contracts verified green

npx tsx scripts/cutover-check.ts --json
# {"timestamp":"2026-08-25T02:47:16.553Z","allPassed":true,"checks":[...]}
```

---

## Hashes para commit / tag

- **Dump custom 2026-08-24:** `cbeadbdf1989d19d0169a580101be2e6496e45c820bdfc768874634c34e88ceb` (`/tmp/pi-backup-2026-08-24.dump`, 155K, `~/backups/pi-financeiro/pi-backup-2026-08-24.dump` na VPS)
- **Schema-only:** `160c79533cdc8ea360a635dac698491d7f641226a8a4341700cc53a2110114c4` (`/tmp/schema.sql`)
- **_migrations checksums:** V032 `7a7a55edaa2939f0d696d981d67c886ad4844371dc119188ee694b33a45a1736`, V033 `c5e4438c0f8fbe042904e2cf750366e3f8b7c7168ed901efc16dc993f330b503`

## Próximos passos (fail-closed)

1. Ajustar `tests/integration/legacy-card-purchases-v032-upgrade.test.ts` para `throw` quando `DATABASE_URL_TEST` ausente (fail-closed em vez de `describe.skip`), e normalizar `set VAR=` Win32 → unset real.
2. Adicionar `--dry-run` a `scripts/rehearse-migration.mjs` (validar dump/manifest sem iniciar container) para o comando do roteiro `DATABASE_URL_TEST=... DB_TEST_MARKER=ci node scripts/rehearse-migration.mjs --dry-run`.
3. `scripts/backup-db.mjs` suportar flags `--vps --out` documentadas no roteiro ou atualizar roteiro para usar `docker exec pg_dump` direto.
