# Goal run — G5.1.2

**Status:** done

## Contract

Authorize active workspace membership before resolving the workspace Durable Object.

## Evidence

- `apps/agent/src/index.ts` calls `authorizeWorkspaceMembership()` before `getAgentByName(env.AGENT, workspaceId)`.
- Authorization forwards the Better Auth cookie and origin to the API workspace-members endpoint; 401/403 are returned without resolving the DO.
- `/health/agent` is static and never resolves a DO; every workspace route is subject to the membership pre-handler.
- `apps/agent/tests/agent-auth.test.ts` proves both authorized resolution and non-member no-resolution.

## Verification

- `pnpm --filter pi-finance-agent test`: 6/6 passed.
- `pnpm --filter pi-finance-agent typecheck`: passed.
- `pnpm --filter pi-finance-agent exec wrangler types worker-configuration.generated.d.ts --config wrangler.jsonc --include-runtime=false --check`: passed.
