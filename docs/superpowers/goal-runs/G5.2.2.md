# Goal run — G5.2.2

**Goal ID:** G5.2.2
**Started:** 2026-08-04T09:20:00Z
**Completed:** 2026-08-04T09:42:00Z
**Status:** done

## Contract

- **Objective:** Emitir token delegado por turno com `iss`, `aud`, `sub`, `workspace`, `role`, `capabilities`, `jti`, `request`, `iat` e `exp` de no máximo 5 minutos; API valida todos os campos.
- **Allowed files:** `apps/agent/src/delegated-token.ts`, `apps/agent/src/index.ts`, `apps/api/src/auth/delegated-token.ts`, `apps/api/src/routes/index.ts`, tests and environment examples.
- **Acceptance:** token HMAC-SHA256 assinado, claims completos, expiração bounded, token entregue somente pelo side-channel do processor e API mapeia a identidade/workspace sem persistir o token.

## Evidence log

| Continuation | Command/result | Failure signature | Decision |
|---:|---|---|---|
| 0 | RED tests added for issuer, verifier and API route integration | missing delegated-token modules | implement |
| 1 | Agent/API typechecks and targeted tests passed | API integration initially timed out without `app.ready()` | add explicit readiness in test |
| 2 | Full API suite passed 559/559; Agent suite passed 23/23 | capability scope test initially used wrong claim names/time | enforce `financial.read`/`financial.write`, use current-time fixtures |
| 3 | Missing secret now fails closed with 503; scope denial returns 403 | — | acceptance hardened |

## Final verification

- [x] Acceptance test
- [x] Impacted tests
- [x] Typecheck/lint
- [x] Diff reviewed; no guard/assertion removed

## Blockers / follow-up

- G5.2.3 must verify token is never persisted or included in model input.
