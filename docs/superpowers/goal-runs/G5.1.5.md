# Goal run — G5.1.5

**Status:** done

Every persisted message carries `actor_id`; every message creation also writes an `agent_actions` audit row with actor, action type, and payload. The Worker derives actor identity from the authenticated Better Auth session after membership authorization, then forwards it in a private internal header to the DO. The DO never accepts a client-supplied actor as authority.

PWA integrates `AgentTranscript` in the profile chat sheet and renders `actorId` above every message returned by the workspace Agent history endpoint. `AgentTranscript.test.tsx` proves two actors are visibly labeled.

Verification: `pnpm --filter pi-finance-agent test` 10/10; agent typecheck and Wrangler types check pass; PWA typecheck and profile tests 17/17 pass. `agent-chat.test.ts` proves two actors create two actor-tagged action rows in shared history and that each separate personal DO remains isolated.
