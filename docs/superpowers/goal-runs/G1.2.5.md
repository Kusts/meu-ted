# Goal run — G1.2.5

**Goal ID:** `G1.2.5`
**Status:** `done`

## Production smoke policy

- Added a manual `workflow_dispatch` workflow requiring an explicit `production_url` input and the protected `production-smoke` environment.
- The workflow runs only Playwright's `production-smoke` project with `E2E_PRODUCTION_SMOKE=1`.
- When production smoke is enabled, Playwright requires `E2E_PRODUCTION_URL` and disables all local fixture API, Next, and SW-harness servers.
- Smoke navigation asserts unauthenticated registration shells and fails if any POST/PUT/PATCH/DELETE request is emitted.
- The release runbook documents post-deploy smoke, explicit confirmation for deploy/rollback, and re-running smoke after rollback.

## Verification

- `node --test scripts/production-smoke-contract.test.mjs`: **3/3 passed**.
- `pnpm --dir apps/pwa typecheck`: passed.
- Existing E2E ownership contract remains covered; no production URL or deploy/rollback command was executed.
- `git diff --check`: passed.
