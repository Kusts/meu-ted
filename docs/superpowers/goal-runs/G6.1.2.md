# G6.1.2 — Reminder server-side (reconciliation P0.7)

**Status: BLOCKED** — integração PostgreSQL não verificada neste ambiente.

## Conflito reconciliado (2026-08-17)

- `docs/goals/2026-08-14-pi-goal-resume.md`: G6.1.2 como `paused` (fonte autoritativa).
- `docs/superpowers/specs/2026-08-14-master-goal-g6-g7-validation-design.md`: listava G6.1.2 como "já concluído".
- Resolução (por plano P0.7 Task 7): G6.1.2 permanece **bloqueado** — a condição `/goal` exige
  integração PostgreSQL real (2 arquivos / 3 testes sem skip), que não roda aqui. Master-design
  corrigido de "concluído" para "bloqueado". P1 NÃO deve assumir conclusão de G6.1.2.

## Evidência P0.7

### Step 1 — Guard fail-closed: PASS

Comando (sem `DATABASE_URL_TEST` / `DB_TEST_MARKER`):

```
pnpm --dir apps/api test:integration
```

Saída:

```
Reminder integration tests require: DATABASE_URL_TEST, DB_TEST_MARKER. Refusing to run with skipped tests.
```

Exit code: **1** (fail-closed, zero skip silencioso). `scripts/require-reminder-integration-env.mjs`
valida protocolo postgres:// e `DB_TEST_MARKER` antes de permitir qualquer teste.

### Step 2 — Integração real: BLOQUEADA (não executada)

Causa: nenhum PostgreSQL **descartável** disponível neste ambiente:

- Docker daemon indisponível (`npipe://./pipe/dockerDesktopLinuxEngine` — aberto falhou).
- Porta 5432 não escuta (`Test-NetConnection 127.0.0.1:5432` → `TcpTestSucceeded: False`).
- `DATABASE_URL_TEST` e `DB_TEST_MARKER` unset.

Sem `DB_TEST_MARKER` não é seguro apontar contra produção; o guard recusa propositalmente.

## Próximo passo para fechar G6.1.2

Subir um PostgreSQL descartável (Docker/local), definir `DATABASE_URL_TEST` + `DB_TEST_MARKER`,
e rodar `pnpm --dir apps/api test:integration` até 2 arquivos / 3 testes passarem sem skip.
Até lá, G6.1.2 fica `blocked` e fora do escopo de conclusão de P1.
