# Goal run — G1.2.4

**Goal ID:** `G1.2.4`
**Status:** `done`

## Gates

- `security:secrets`: cross-platform Node wrapper around pinned `zricethezav/gitleaks:v8.24.2`, scans the full Git history, uses a read-only mount, redacts output, and fails on findings.
- `security:deps`: `pnpm audit --prod --audit-level=moderate` against the real `pnpm-lock.yaml`.
- `security:containers`: cross-platform Node wrapper around pinned `aquasec/trivy:0.58.1`, scans both CI images with `HIGH,CRITICAL`, `--exit-code 1`, and a persistent Trivy cache volume.

## Remediation

- Updated Pi Agent to `^0.83.0`, Fastify to `^5.10.0`, and Serwist packages to `9.5.12`.
- Added lockfile overrides for patched `fast-uri`, `find-my-way`, `postcss`, `sharp`, `brace-expansion`, and `protobufjs` versions.
- Runtime Docker images now contain production dependencies only; build-only package managers/stores are excluded from the final scan surface. Bridge `tsx` is a runtime dependency because its entrypoint is TypeScript.
- Gitleaks allowlist is limited to three deleted historical test fixture paths containing deterministic identifiers; current source remains fully scanned.

## Verification

- `node --test scripts/security-gates.test.mjs`: **10/10 passed**.
- `pnpm security:deps`: **No known vulnerabilities found**.
- `pnpm security:secrets`: **250 commits scanned; no leaks found**.
- `pnpm security:containers`: API image **0 HIGH / 0 CRITICAL**; Pi bridge image **0 HIGH / 0 CRITICAL**.
- API typecheck and tests: **297/297 passed**.
- Bridge typecheck and tests: **447/447 passed**.
- Docker runtime layout checks passed for both images.
- `git diff --check`: passed.
