# Goal run — G5.1.1

**Status:** done

## Contract

Create `apps/agent` with a Cloudflare Durable Object Worker, an `AGENT` binding mapped to `WorkspaceAgent`, and a versioned SQLite migration.

## Evidence

- `apps/agent/wrangler.jsonc`: Worker `pi-finance-agent`, Durable Object binding `AGENT`, class `WorkspaceAgent`, migration tag `v1` with `new_sqlite_classes`.
- `apps/agent/migrations/0001_workspace_agent.sql`: version-1 `agent_state` and `messages` tables plus actor/timestamp indexes.
- `apps/agent/src/index.ts`: Worker entrypoint and `WorkspaceAgent` class; static `/health/agent` reports binding readiness without resolving a DO, while workspace requests return membership-required until G5.1.2 pre-authorization.
- `apps/agent/src/schema.ts`: executable version-1 schema initializer with `_agent_schema_migrations` marker and idempotent table/index creation; constructor runs it before serving health.
- `apps/agent/worker-configuration.generated.d.ts`: generated by Wrangler from the binding config.
- `apps/agent/tests/agent-scaffold.test.ts`: binding, migration, and export contract.

## Verification

- `pnpm --filter pi-finance-agent test`: 4/4 passed, including runtime schema execution and binding health dispatch.
- `pnpm --filter pi-finance-agent typecheck`: passed.
- `wrangler types ... --check`: passed with Wrangler 4.104.0.
